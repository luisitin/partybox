# I-065 · Sound off stills the room — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-065.md` (the owner's pick, branch `b/I-065-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the shell writes `data-sound` on `<html>`
and the lobby's glows pause, grey and dim while sound is off (A); they swell once as it comes on
(B) and the "Scan to join" heading pops with the wake (C); the wake's motion twins added.

## Proof (2026-09-22, `gate-probe.ts` against this worktree)

- Before the tap: `sound=off`, glow `opacity 0.1`, `filter grayscale(1)`, `animation-play-state
paused`.
- Just after: `sound=on`, opacity mid-swell `0.196`, filter none, both animations running.
- Settled: `opacity 0.18` — the drift's resting level.
- `pnpm verify` GREEN.
