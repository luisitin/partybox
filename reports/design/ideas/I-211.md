# I-211 B — the drawing on the show fills its stage

Shipped from `b/I-211-B` (commit `362d6cf0`, cherry-picked clean onto `session-c`), no redesign.

- **A** — the book's title and page count move from the header row into the left column above the
  WORD tile, so the page area gets the header row's height.
- **B** — "X drew" is a pill on the sheet's top-left corner and the 560 px cap is gone: the sheet is
  `min(100cqh − 16px, 100cqw)` of the page area.
- **The owner's note** ("the text under 'Bot 1's Book' is a bit crammed — make it smaller or shorten
  it"): shortened. The branch's kicker "BOT 1 TURNS THE PAGES · BOOK 1 OF 6 · PAGE 2 OF 5" wrapped to
  three ragged lines in the 300 px column; the title already names who turns the pages (the owner
  presents their own book), so the column now keeps two short facts, one per line, never wrapped:
  "BOOK 1 OF 6" / "PAGE 2 OF 5" (Spanish "LIBRO 1 DE 6" / "PÁGINA 2 DE 5"; the page string was
  already in the table, `book {book} of {books}` is new; the old long string is dropped).

## Proof (probe `pencil5.tmp.ts`, port 42221; scratchpad `c-capture/pencil5/`)

Sam (iPhone) + Priya (Pixel) + fast bots, seed 3, passes 2, TV 1920×1080, on Priya's drawing page.

- Six players: sheet **648 × 648 px** (main ≈ 480, the branch's clip ≈ 650); bottom 938, the host
  bar's End game at 1016. The "Priya drew" tag at (814, 302) inside the sheet's corner (802, 290).
- Eight players (two roster rows): sheet **589 × 589**, bottom 943 vs host bar 1016; tag inside the corner.
- Page count: two spans, "BOOK 1 OF 6" and "PAGE 2 OF 5", each 39 px tall at a 28 px font — one
  line each — inside the column.
- Guess page: "Bot 4 guessed / ???" keeps its size, centred in the page area.
- Screenshots looked at: 6-player drawing (sheet centred, tag on the corner, column un-crammed),
  6-player guess, 8-player word page (title + two lines, word centred).
- `capture-pencil-show-fit.ts` (the review-loop's fit capture) now finds the page count by its new
  text and checks the tag sits on the sheet instead of under the old kicker.

Not run: the spec's `C:/dev/partybox-ideas/capture/pencil.ts` recording writes under
`partybox-ideas/captures`, which this build session may not write to; the DOM probe above settles it.

`pnpm verify` green (the unrelated flaky `push-dedupe.test.ts` failed once under load, passed 3/3
alone and on the full re-run).
