# I-158 · Blanks · one Pick 2 / Pick 3 card mid-game — shipped option B

Branch `b/I-158-B` applied as is (cards.ts, index.ts). Session B's open question — must "exactly one"
also hold in judge mode, where the judge picks among candidates? — needs the owner; not decided
here, so judge mode keeps the branch's behaviour.

## Proof

- `doubles.test.ts`: 40 shuffles × every deck preset — a six-round game's only multi-blank card is
  round 4; a fifteen-round game has one in round 8.
- A real six-round WILD game on this worktree (probe): rounds 1–6 read Pick one, Pick one, Pick one,
  **Pick 2, in order**, Pick one, Pick one (`captures/blanks/I-158-shipped/r4-phone.png`).
