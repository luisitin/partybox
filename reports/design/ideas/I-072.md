# I-072 · The QR code is sized for the couch — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-072.md` (the owner's pick, branch `b/I-072-C`, no note).
Cherry-picked onto `session-c` as the branch has it (a CSS append conflict with I-049, both kept):
560 px while nobody has joined (A), the first join shrinking it over 600 ms, transform only, rest
= end state (B), the QR panel alone in the middle of the stage until the first player (C). The
shrink's motion twins added.

## Proof (2026-09-21, the ideas lobby capture against this worktree)

- `I-072/lobby.png` — the `lobby` clip, the owner's `I-072-C` (rows 1 and 3) and mine (rows 2
  and 4): the code big and alone, Sam's join shrinking it as the roster column appears, then the
  bots. The same beats (my badge is I-049's, shipped since the owner's clip).
- Cue log: `ready`, then a `join` per player; nothing new.
- `pnpm verify` GREEN.
