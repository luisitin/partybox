# I-001 · Wisecrack answer page reacts while you type — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-001.md` (the owner's pick, branch `b/I-001-C`). Reapplied on
`session-c`: same CSS (the field's danger border + beat under `data-urgent`, the counter remounted
per keystroke with `pb-bump`, an empty field breathing after 3 s), one change to the code:

- The stall flag is armed by the timer and cleared in render when a character lands (the
  branch's `setStalled(false)` inside the effect body trips the repo's `react-hooks` rule
  "setState synchronously within an effect"), so `pnpm lint` stays green. Behaviour is the same:
  a field emptied again waits its 3 s afresh.
- Explicit `data-motion='off'` twins for the beat and the breath (the tokens already collapse).

`TextAnswer` is game-sdk, so Broken Pencil's text field gets the same behaviour (noted in the
CHANGELOG and DESIGN_SYSTEM).

## Proof (2026-09-20, the ideas Wisecrack capture `--edge` against this worktree)

- `cmp-edge.png` — the last 12 s with the second answer left empty, mine (top) beside the owner's
  `I-001-C` clip (bottom), frames 20/60/100/120/130/140: the empty field dims in its 2 s breath
  before HURRY, takes the danger border at 4 s and beats with the digits — beat for beat.
- `cmp-typing.png` — the first answer typed: the same frames on both; the counter steps per
  keystroke (`counter.png`, frames 40–55: 6 → 16 / 80).
- `pnpm verify` GREEN.
