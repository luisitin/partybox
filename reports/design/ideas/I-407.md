# I-407 · bingo · the style preview is one slim row and confirms itself at 20 s — shipped option B

Branch `b/I-407-B` cherry-picked clean onto `session-c` (1cf7e73a). One fix on top: on the branch
the slim bar's Confirm ran off the right edge (iPhone SE: Confirm's right edge at 317 px in a
320 px viewport, past the bar; Pixel 7 at 200 % text: 447 px in 412 px, "Confi" cut). The label
now gives instead of the buttons — `min-width: 0` on the label, the word in its own span with an
ellipsis, the style's mark kept whole. The README's I-013 C "1.5 × mark" clause is replaced by the
I-407 A/B line (the slim bar drops the big mark, as on the branch).

## Proof (probe on port 42221; blackout, 2 cards, callSeconds 6; four phones)

| device                 | bar (top, height) | cards' bottoms | Change / Confirm right edge | label                        |
| ---------------------- | ----------------- | -------------- | --------------------------- | ---------------------------- |
| iPhone SE 320 px       | 500, 52           | 280, 460       | 201 / 296                   | `S…` + mark                  |
| iPhone 393 px          | 591, 52           | 298, 517       | 274 / 369                   | `Stack?`                     |
| Pixel 7 200 % 412 px   | 771, 52           | 373, 682       | 246 / 388                   | `S…` + mark                  |
| iPhone, Spanish locale | 591, 52           | 294, 513       | 259 / 369                   | `¿Pila?`, Cambiar, Confirmar |

Both cards end above the bar on every device (main's ~200 px block hid the second card's last
three rows). Priya (SE) left the Stack preview up untouched:

- the room is held the whole time: phase `play`, `deadline: null` at 0.6 s and 10.6 s; the TV
  shows "Priya is changing card style… calling resumes when they are done";
- the drain line's `scaleX` is 0.963 at ~0.7 s and 0.457 at ~10.9 s (linear over 20 s);
- the bar is gone ~20.0 s after the Stack tap (still up at 19.9 s, gone by 20.0 s), the TV shows
  the 3 · 2 · 1 ("calling resumes in"), then re-calls G 53 ("Fifty-three — Herbie the Love Bug",
  still call 2 of 75) with a fresh 6 s deadline; Priya's phone is on Stack with the style pill back.

`pnpm verify` green (one unrelated timing flake in `push-dedupe.test.ts` on the first run, green
alone and on the rerun). The ideas capture (`capture/bingo.ts` into `partybox-ideas/captures`) was
not run: this session writes nothing under `C:/dev/partybox-ideas`.
