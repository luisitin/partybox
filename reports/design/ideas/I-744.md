# I-744 · lobby · surviving a server restart — shipped option B

Branch `b/I-744-B` cherry-picked cleanly, plus:

- The phone's "The party started over — the room is VMDJ now…" and the TV banner were English-only:
  now `t.join.restartedTo(code)`, `t.tv.restartedBefore` / `restartedAfter`, with Spanish.
- **TV banner layout fix**: `left: 50%` capped the banner's shrink-to-fit width at half the screen,
  so at 1080p it wrapped to two lines (y 130–228) and covered the top of "Scan to join" (y 208).
  `width: max-content` (still `max-width: 80vw`) keeps it on one line: y 130–192, the heading
  starts at y 208.

## Proof (probe on port 42191: the server stopped for 6 s and started again on the same port)

| moment                       | what showed                                                                                                                 |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| before                       | room ZNJS; Priya (iPhone SE, `/?room=ZNJS`) and Lucía (phone in Spanish) in                                                 |
| after restart, Priya's phone | URL now `/` (dead code dropped), room box `VMDJ`, "The party started over — the room is VMDJ now. Tap Join to get back in." |
| after restart, Lucía's phone | "La fiesta empezó de nuevo: ahora la sala es VMDJ. Toca Entrar para volver."                                                |
| after restart, TV (1080p)    | banner "PartyBox restarted — this is a new room, VMDJ. Phones: tap Join to get back in.", one line, x 424–1496              |
| Priya taps Join              | in VMDJ's lobby as VIP (first one back); the TV banner is gone                                                              |

Seen, not changed: after the restart the reloaded TV shows "Tap anywhere for sound" over the bottom
of the QR card, as on main. `pnpm verify` green.
