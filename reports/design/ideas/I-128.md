# I-128 · An all-zero board is not a tie — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-128.md` (the owner's pick, branch `b/I-128-C`, no note).
Cherry-picked onto `session-c` as the branch has it: "No bingos this time" / "Nobody scored" with
"The game ended before anyone could." (A), a blank rank column — `Scoreboard` `noRanks` (B), a
quiet `leave` instead of the cheer (C). The results-rows test now pins the honest copy.

## Proof (2026-09-21, the ideas Bingo capture with ENDEARLY=1 against this worktree)

- `I-128/end-early.png` — `end-early-tv` and `end-early-phone`, the owner's `I-128-C` and mine:
  two calls in, Sam ends the game — "No bingos this time", the all-zero board without ranks on
  the TV. Frame for frame.
- Cue log at the mark: `call −0.14 · leave 1.52` in both; no cheer.
- `pnpm verify` GREEN.
