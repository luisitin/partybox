# I-156 · Blanks · the vote never pages before stepping down — shipped option C

Branch `b/I-156-C` cherry-picked cleanly (1cc41eef, `TvJudge.tsx` only). One small addition: the
answers-only card past eight writes one `____` per white (the branch wrote a single one), so a Pick 2
keeps both answers inline instead of spilling the second into the card's "extra" line.

## Proof (probe on port 42171, 1920 × 1080 TV, samples at 1.5 s / 4 s / 7.5 s into `judge`)

- Czar, 5 players → 4 cards: 2 × 2 (two rows, 852 px cards), all 4 lit and fully on screen at every
  sample, no page turn.
- Vote, 8 players, wild → 8 cards: 4 × 2 at `mini` (414 px), all 8 lit, one page, full sentences.
- Vote, 12 players, wild → 12 cards: 4 × 3 at `mini`, the setup blanked — each card reads only its
  answer ("Stalin's dick pic collection."); all 12 lit from 4 s on (the grid settles from 5 rows at
  1.5 s to 3 rows by 4 s as the step-down latches), no page turn.

## Deviations

- The ideas capture tool (port 42145, writing into partybox-ideas/captures) was not run; this
  builder's port is 42171 and it does not write into the ideas folder.
