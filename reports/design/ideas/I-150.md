# I-150 · Blanks · the fill reads as one sentence — shipped option C

Branch `b/I-150-C` cherry-picked clean: `fill()` lowers a card's everyday opening capital
mid-sentence (a gerund or a listed opener; names, acronyms and "I" keep theirs — A), steps a
quoted card's double quotes down to single ones inside a quotation (B), and `join.test.ts` sweeps
every quoted wild blank against every quoted white card for doubled or unbalanced quotes; the card
of the night's award line quotes through `innerQuotes()` (C). The shipped `game.test.ts`
expectations that pinned the old capitals move with it, as in the branch.

One tidy-up: the branch dropped its new constants between the `ABBREVIATION` doc comment and its
constant; the comment is back above the constant it describes. README Content names A/B/C.

## Proof

Probe on port 42161 (TV 1080p, vote mode, 2 phones + 4 bots, the vote grid read on the TV):

- Seed 7, "The school's field trip bus came back with ____.": "…with **the** Roomba ate the thong."
  and "…with **the** raccoon has a library card." come down; "Viagra and a heart condition.",
  "Stalin's dick pic collection.", "Madeleine McCann.", "Pizza Rat." keep their capitals.
- Seed 11, a question card ("Who taught the parrot that word?"): every answer keeps its capital
  (the blank follows a "?").
- Blanks 140 tests incl. the deck sweep (>100 quoted blanks × >100 quoted cards, no doubled
  quote); `pnpm verify` green (660 tests).
