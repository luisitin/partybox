# I-153 · Shell results headline · people first, bots folded — shipped option C

Branch `b/I-153-C` applied: `winnerLine()` sorts people before bots (A), folds tied bots into
"& the bots" when people share the top (B), and says "The bots tie — nobody home?" when only bots
tie, while a lone bot winner keeps its name (C).

Merge: the branch added its two strings to the old single-file `i18n.ts`; main has since split it
into `i18n-en.ts` + `i18n-es.ts` behind a language proxy. `i18n.ts` kept as main has it; the strings
went into both tables — Spanish "¡Empate entre Priya, Sam y los bots!" / "…y el bot!" and "Empatan
los bots: ¿no hay nadie en casa?". Three new `results-rows.test.ts` cases (mixed tie, one bot,
all-bot tie, lone bot winner, people-only tie unchanged). DESIGN_SYSTEM.md and the Blanks README
(Scoring) name A/B/C.

## Proof

Probe on port 42161: a real 3-round Blanks game driven to `final`, the scores rewritten through
`/api/dev/load-state` into the tie wanted, then skipped to `done` (a tie is luck of the deck).

- Mixed tie (Sam, Priya, Bot 1, Bot 3 on 3; Lucía, Bot 2 on 1): TV headline "Priya, Sam & the bots
  tie!" over four faces; Lucía's phone the same line, Sam's and Priya's "You tie for first! 🏆";
  ES Lucía "¡Empate entre Priya, Sam y los bots!", two lines, no clipping on the iPhone SE.
- Bots-only tie (Bot 1, Bot 3 on 3): both phones "The bots tie — nobody home?".
- `pnpm verify` green (663 tests).
