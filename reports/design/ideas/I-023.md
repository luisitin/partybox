# I-023 · Broken Pencil's tiers are a spice dial — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-023.md` (the owner's pick, branch `b/I-023-C`). Reapplied on
`session-c` as the branch has it, with the offers list moved into `client/Offers.tsx`
(`Controller.tsx` was over the 300-line cap) and a `data-motion='off'` twin for the ember. The
phone view gains `spicy` (additive; the README's phase table names it).

## Proof (2026-09-20, the ideas Broken Pencil capture against this worktree)

- `cmp-pick.png` — the `pick-phone` window, mine (top) beside the owner's `I-023-C` clip
  (bottom): the join, then the three offers with one green, two amber and three red discs over
  the captions, then the pad (ruled here since I-021 shipped after the owner's clip).
- Cue log: `submit` on the pick — the shell's, unchanged.
- `pnpm verify` GREEN.
