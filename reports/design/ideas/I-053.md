# I-053 · The host bar advertises the shelf — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-053.md` (the owner's pick, branch `b/I-053-B`, no note).
Cherry-picked onto `session-c` as the branch has it: the game button's label turns over every
2.2 s with a keyed fade (A) and a glyph per game (B); the fade's motion twins added.

## Proof (2026-09-22, `hostbar-probe.ts` against this worktree)

- The button read every 2.2 s: `🎮 Pick a game → 🎱 Bingo → ✍️ Blanks → ✏️ Broken Pencil →
⚡ Lightning Round → 😂 Wisecrack → 🎮 Pick a game` — the whole shelf and back.
- `pnpm verify` GREEN.
