# I-020 · Submissions visibly arrive — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-020.md` (the owner's pick, branch `b/I-020-C`, with the note
"guarantee smoothness and analyze the sounds (seems like too many beeps and not smooth)").
Cherry-picked onto `session-c`, plus the note:

- **Sounds**: one note per landing. The branch had the shell's `lock` at the push AND the `card`
  pluck 330 ms later (ten notes in eight seconds with five players). Kept the pluck at the landing
  beat; the shell's `lock` is quiet in a phase the game lists in a new additive
  `GameClientModule.ownLocks` (Blanks: `['answer']`). The "Everyone's in!" turn-up adds no note.
- **Smoothness**: `packages/e2e/src/design/capture-blanks-table-smooth.ts` (in the repo) samples
  every landed pip's computed opacity and translate every frame through the drop, the puff beat
  and the turn-up, and checks the turn-up runs on the same element (never a second drop).
  **17 / 17**: largest opacity step 0.21 (the drop's first frame), largest translate step 12.9 px,
  every pip ends flat and opaque; the turn-up's animation name follows the drop's on the same run.
- Motion twins for the puff and the turn-up (reduced motion / `data-motion='off'`).

## Proof (2026-09-21, the ideas Blanks capture against this worktree)

- `I-020/answer-tv.png` — the `answer-tv` clip, the owner's `I-020-C` (rows 1 and 3) and mine
  (rows 2 and 4): the slots fill in roster order with faces, "Everyone's in!" turns the row up.
- `I-020/table-turned.png` — the probe's still after the turn-up.
- Cue log at the mark: mine `phase −0.30 · card 1.97 · 2.85 · 3.26 · 3.96 · 6.33` (five landings,
  five notes), then the reveals' `card`; the owner's `phase −0.30 · lock 0.68 · card 1.02 · lock
1.68 · card 2.02 · lock 3.60 · card 3.94 · card 4.50 · lock 5.91 · card 6.25` (two per landing).
- `pnpm verify` GREEN.
