# I-086 · lobby · pick your avatar's colour — shipped option C

Branch `b/I-086-C` applied (4 files; the import lines were re-merged onto main), plus:

- **Session B's note kept**: the eight swatches sit ABOVE the face grid and in one row on any phone
  (a grid of eight up to 36 px; the branch's 36 px flex row wrapped 6 + 2 on an iPhone SE, its
  second row under the "more below" chevron). The swatches and the colour logic moved to
  `JoinTints.tsx` (Join.tsx was over the 300-line limit), the rotating name placeholder to
  `useExampleName.ts`.
- Spanish for the swatch labels (`t.join.pickColour`, `t.join.colour`); a `fox#3` face keeps its
  screen-reader name (`avatarName` reads the face).
- Found while proving it: on an iPhone SE the lobby's "Lobby" title slid under the 🚪 Leave pill (the
  pills never shrink). The title row now wraps, pills right-aligned on their own line.

## Proof (probe on this worktree; screenshots in `captures/lobby/I-086-shipped/`)

| phone     | viewport | swatch row         | face grid starts | picked   | joined as |
| --------- | -------- | ------------------ | ---------------- | -------- | --------- |
| iPhone SE | 568 px   | y 418–451, one row | y 467            | colour 4 | `dino#3`  |
| iPhone 15 | 659 px   | y 422–458, one row | y 474            | colour 7 | `bee#6`   |

The portrait recolours on the tap (pink → blue on the SE), the lobby chip and header avatar wear the
colour, and on the iPhone 15 the colour Priya already wears is crossed through (C). `ids.test.ts`
pins `avatarFace` / `avatarTint`; `pnpm verify` green.
