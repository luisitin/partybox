# I-105 · Bingo · after a bingo every phone votes — shipped option C

Branch `b/I-105-C` applied (13 files: the vote in the bingo phase, its tests, the phone's buttons
with the counts on them, the TV's vote list, the clock). Session B's note asked to confirm C with
the owner; the pick is recorded as C with no note and Session C does not ask, so C ships — flagged
here and in the hand-off.

Re-merged onto main: the vote footer, tally and clock moved to `Vote.tsx` (ControllerParts.tsx was
over 300 lines), everything in Spanish; the TV's decide line now reads "Everyone votes on their
phone: …" (was "Anyone picks…"), and the phone's win screen says "Everyone votes." where the branch
still said "Anyone can pick." — the old first-tap wording would now be wrong.

Session B's second note, the verdict screen at 1080p: a win with stray daubs carries the "…never
called — lucky the line was real" kicker, and with the vote's clock and list the column ran past the
claim stage (the list was clipped). That win now steps its column down (`.winTight`: BINGO! 80 px,
the headline h2, the kicker a caption, the decide line a caption).

## Proof

- Capture from this worktree (`VOTE=1`): `captures/bingo/I-105-shipped/`. `vote-phone` f030 is the
  owner's `I-105-C-vote-phone` f030 line for line: "✓ Keep going — blackout · 1", "Vote closes in 5
  — tap another to change it". The vote ends 1–1, the VIP's vote wins: "Next: round 2 — Four
  corners" (tv frame at 76.5 s), as in the owner's recording.
- 1080p, 4 players, a stray daub, Priya voted (probe): card bottom 902, vote list bottom 915, claim
  stage bottom 946, host bar 1008 (was: list 988 against 946, clipped) — `tv-1080p-stray-vote.png`.
- Bingo tests 85/85 (the branch's `vote.test.ts` and updated rules); `pnpm verify` green.
