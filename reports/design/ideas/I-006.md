# I-006 · Bingo's wrong claim is a moment — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-006.md` (the owner's pick, branch `b/I-006-B`). Reapplied on
`session-c` as the branch has it. One note against the spec's contract section: `WIPE_AT_MS` in
`server/reveal.ts` is a client-read timing constant beside the reveal's others — nothing crosses the
wire, no view field, so the README's phase table names it as a beat, not a field.

## Proof (2026-09-20, the ideas Bingo capture against this worktree)

- Cue log of the `check-wrong` window is identical to the owner's clip: dibs 1.10, call 1.26,
  reveal 1.51, wrong 7.87, ticks 10.86 / 11.86 s.
- `cmp-wrong.png` — frames 60–96, mine (top) beside the owner's `I-006-B` clip (bottom): the same
  turn, the same verdict, then the daubs gone by frame 96 on both.
- `cmp-wipe.png` — the card at frames 78–81 (the shake under the buzzer: the card sits 10 px off
  on frames 79–80 on both) and 89–94 (the wipe: 1.2 s after NOT A BINGO the first cells drain in
  reading order on the same frame on both; FREE and the dashed misses stay).
- `pnpm verify` GREEN.
