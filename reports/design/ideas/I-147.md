# I-147 · Blanks · a tie is played off — shipped option A

Branch `b/I-147-A` applied (11 files, with `tiebreak.test.ts`). Re-merged onto main: the TV round
card's TIE-BREAK and the sitting-out phone's lines in Spanish.

## Proof

Capture from this worktree (`FINAL=1 TIE=1`: the last round's scores forced level for Sam and
Bot 1, Priya below): `captures/blanks/I-147-shipped/`, against the owner's `I-147-A-tie`.

- `tie-break-phone` (Priya, outside the tie): "Tie-break · Only the tied players play this card —
  you vote on it." with two slots — as A's.
- `tie-break` f110: "Play a card from your hand · 1 / 2 in · Just waiting for Sam…" with Bot 1 and
  Sam on 3 — as A's f110 (only the tied players play).
- Blanks 152 tests incl. `tiebreak.test.ts`; `pnpm verify` green.
