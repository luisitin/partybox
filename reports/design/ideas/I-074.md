# I-074 · Removing your bot is a poof — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-074.md` (the owner's pick, branch `b/I-074-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the tap holds the remove 450 ms while the row
shrinks and four 8 px bits fly out (A), a quiet `leave` on the phone as it puffs (B), the rows rise
into place 30 ms apart when the count changes (C). Motion twins for the poof, the bits and the
settle. Found on the way: the recorded-phone opener (`openPhoneRecorded`) still typed the bare URL
after I-041, so B's capture could not press Join — every phone opener now shares `phoneUrl()`.

## Proof (2026-09-21, the ideas lobby capture with BOTS=1 against this worktree)

- `I-074/bot-phone.png` — the `bot-phone` clip, the owner's `I-074-C` (top) and mine (bottom):
  Priya adds a bot, holds its row, removes it. `I-074/settle.png` — the rows after the poof with
  the "Priya's bot left" toast, the list settling.
- Cue log at the mark: mine `join −0.09 · join 1.62 · leave (phone, quiet) 5.23 · leave (TV)
5.71`; the owner's `join −0.06 · join 1.54 · leave (phone) 5.07 · leave (TV) 5.53` — the
  phone's puff note, then the TV's leave 0.46–0.48 s later in both: the 450 ms hold.
- `pnpm verify` GREEN.
