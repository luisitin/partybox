# I-069 · Muting the TV says so — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-069.md` (the owner's pick, branch `b/I-069-A`, no note).
Cherry-picked onto `session-c` as the branch has it: `AudioGate` reports a manual toggle and the
shell raises "Sound off" / "Sound on" through the TV's own toast list (I-054 C's helper, 3 s).

## Proof (2026-09-22, `mute-probe.ts` against this worktree)

- Tapping the header's Mute: the toast list reads `Sound off`; tapping again: `Sound on` (the
  previous one still fading, as the 3 s list does).
- `pnpm verify` GREEN.
