# I-070 · A waiting player can nudge the VIP — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-070.md` (the owner's pick, branch `b/I-070-C`, no note).
Cherry-picked onto `session-c` as the branch has it: a `nudge` socket message → engine event →
one toast to the room naming the sender, with a 20 s cooldown on the phone and 10 rate-limit
tokens on the server (A); the VIP's phone buzzes and plays `phase` (B); the TV rocks the sender's
chip — `PlayerChips` `wavingIds` (C). The wave's motion twins added; the message is in
docs/PROTOCOL.md (with the VIP actions from ADR-040/041).

## Proof (2026-09-22, `nudge-probe.ts` against this worktree)

- The pill shows on Priya (not the VIP) and not on Sam (0 matches).
- One tap → TV and Sam's phone both read "👋 Priya says: hurry up, Sam!"; one chip is waving on
  the TV; Priya's pill reads "👋 Nudged" and is disabled (the 20 s cooldown).
- `pnpm verify` GREEN.
