# I-752 · The phones stop downloading the questions and the decks — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-752.md` (the owner's pick, branch `b/I-752-C`). Cherry-picked
clean onto `c/lobby-b` (commit `292cfd63`), plus prettier on the two new tests and one README line
per game.

- A: Lightning's `wagerAmount` / `clampWager` move to `server/wager.ts` (no content import);
  `scoring.ts` re-exports them, so server callers are unchanged. `CustomStake.tsx` imports `wager.ts`.
- B: Blanks' `fill` / `fillText` / `glue` / `Segment` move to `server/fill.ts` (imports only
  `content/schema`); `cards.ts` re-exports them. `Cards.tsx`, `TvJudge.tsx`, `TvReveal.tsx` import `fill.ts`.
- C: `__tests__/phone-bundle.test.ts` in both games walks every value import reachable from
  `client/` and fails if one reaches `server/content` or a `.json`.

## Proof (2026-09-24, port 42201, iPhone SE phone + 1080p TV)

- Production build (`pnpm verify` → build): 26 JS assets, the largest game chunk is 25 KB
  (`Controller-*.js`); `answerIndex` appears in **0** of them (Session B measured 1,180 KB with
  3,868 on main before).
- Dev-server probe: the SE phone made 33 requests through a Lightning game and a Blanks round; the
  only content-folder module it loaded is `games/blanks/content/schema.ts` (the `____` constant and
  zod shapes, no deck).
- Lightning wager screen on the SE (Sam at 5,715 points): "Wager part of your 5715 points",
  presets 0 / 1420 / 2850 / 4280 / All in 5715, and the custom-% row with Place — as on main.
- Blanks: the vote list on the phone and the TV's result card fill correctly ("The Mona Lisa is
  smiling about | A Midwest key party with a hotdish."), paper marks as before.

Not run: the ideas `capture/blanks.ts` recording on port 42145 (another session's port, and it
writes into `C:\dev\partybox-ideas\captures`, which this session must not touch). The change moves
code without editing it, so nothing on screen can differ.
