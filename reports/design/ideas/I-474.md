# I-474 A: a tied award goes to someone without one yet (shipped)

Owner's pick, 2026-09-24: "The night's leader takes every tied award; one player can sweep all
three." Option A: **a tied stat goes to the player with no award yet, and only then to the higher
score.** This is Session B's branch `b/I-474-A` cherry-picked unchanged: `leader()` takes a `won`
set in Blanks and Wisecrack, and the award order stays the same. Lightning is not changed.

## Proof

- Unit: `games/blanks/__tests__/award-spread.test.ts`. Priya has 5 votes to Sam's 4 and they tie
  3–3 on fast cards. Crowd favourite goes to Priya and Quick draw goes to Sam. When Priya's stat
  is outright higher (5–4), she still takes Quick draw.
- `pnpm verify` is GREEN: registry, typecheck, lint, lint:deps, format, unit+contract, sim smoke,
  build and doc drift. On the first run, `packages/server/src/push-dedupe.test.ts` (I-750) failed
  once under load. It passed 3 of 3 times on its own, and the full re-run was green. It does not
  touch awards.
- Headless probe `i474-awards.tmp.ts` on port 42231 (moved to the scratchpad's
  `c-capture/blanks5/`). The probe runs a real Blanks game (3 rounds, timed, voting) with Sam
  (iphone), Priya (iphone-se), Ana (pixel), Ben (font200) and one bot. On the last round's result
  it loads the SWEEP stats, the same ones the ideas capture uses: votes Priya 5 / Sam 4, fast cards
  3 / 3, Priya's card of the night, scores 5 / 4. The final results screen shows:

  | award             | TV    | Sam's phone                               | Priya's phone            |
  | ----------------- | ----- | ----------------------------------------- | ------------------------ |
  | Card of the night | Priya | "Card of the night Priya"                 | "Your card of the night" |
  | Crowd favourite   | Priya | "Crowd favourite Priya"                   | "Your crowd favourite"   |
  | Quick draw        | Sam   | "Your quick draw" (3 cards in under 30 s) | "Quick draw Sam"         |

  On main, Quick draw went to Priya on score, so she held all three awards.

- Screenshots checked for the TV, iphone, iphone-se, pixel and font200. The award cards and chips
  have the same count and layout as before, and nothing is clipped. The only change is the name
  on Quick draw.

## Not touched

The choreography, sounds and layout are unchanged. The only change is which player the server
names on an award. Wisecrack's Crowd favourite, Sweep master and Speed writer use the same rule.
Unit tests cover it, but it was not filmed.
