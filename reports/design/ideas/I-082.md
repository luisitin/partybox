# I-082 · A first-time VIP gets three tips (option C)

Branch `b/I-082-C` reapplied (`Lobby.tsx` / `Lobby.module.css` conflicted against the Share pill and
S-003's setup sheet). The branch put the localStorage helpers inside `Lobby.tsx` and had `VipMenu`
import them from there; they live in `controller/vipTips.ts` instead so the menu does not import the
lobby. The strip's fade also got its reduced-motion and `data-motion='off'` twins.

## What ships

- **A** — under "You're the VIP", only for the first room this browser has ever hosted
  (`partybox:vip-tips-seen`): three tips rotating every 5 s; ✕ ends them for good.
- **B** — "Add bots" retires once a bot is in; the strip ends when nothing is left to say.
- **C** — a 💡 in the ★ VIP menu clears the flag for the next host who borrows the phone.

## Proof

Scratchpad `c-capture/probe_I082.ts`, Sam's phone as the first host in a fresh browser:

```
t=0      💡 Add bots to fill empty seats — they play for real. ✕
t=5.2s   💡 Tap your ★ VIP badge for pause, lock and kick. ✕
t=10.4s  💡 Recaps save to the host PC — switch it off on the game picker. ✕
after a bot, over 20 s:  💡 Tap your ★ VIP badge… || 💡 Recaps save to the host PC…
after ✕       none
after reload  none
flag          1
```

Three tips at 5 s each, the bots tip gone once a bot is in (only the other two over four rotations),
✕ dismissing it for the session and the reload. `pnpm verify` GREEN (120 s).
