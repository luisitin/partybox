# I-050 · The connection dot has a heartbeat — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-050.md` (the owner's pick, branch `b/I-050-A`, no note).
Cherry-picked onto `session-c` as the branch has it: the connected dot breathes, opacity 0.55 ↔ 1
over 2.4 s, still under reduced motion; the `data-motion='off'` twin added.

## Proof (2026-09-21, `dot-probe.ts` on a real phone against this worktree)

- The dot's computed opacity every 100 ms over 2.5 s: `0.85 0.77 0.71 0.65 0.60 0.57 0.55 0.55
0.57 0.60 0.65 0.71 0.79 0.85 0.91 0.96 0.99 1.00 1.00 0.98 0.94 0.90 0.83 0.75 0.69` — one
  full breath in 2.4 s between 0.55 and 1, ease-in-out (a 10 fps clip cannot show a 2.4 s
  breath; the numbers can).
- Under `prefers-reduced-motion: reduce`: `animation-name: none`, opacity 1 — still.
- The `join-phone` clip is the one the I-049 recording made minutes earlier on this same shell
  (gold → green on connect, unchanged).
- `pnpm verify` GREEN.
