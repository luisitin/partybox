# I-066 · A phone screen says there is more below — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-066.md` (the owner's pick, branch `b/I-066-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the fold fades 72 px — one cut row (A); a ▾
pill sits at the body's foot while it can scroll, on every phone Screen (B); tapping it scrolls a
screen down (C). The pill's motion twins added; noted in docs/DESIGN_SYSTEM.md.

## Proof (2026-09-22, `fold-probe.ts` on an iPhone SE against this worktree)

- The join page: one ▾ pill, `--pb-fold: 72px`; `I-066/se-top.png` shows the faded fold.
- Tapping the pill: `scrollTop 0 → 312`; scrolled to the end, the pill is gone (0 matches).
- `pnpm verify` GREEN.
