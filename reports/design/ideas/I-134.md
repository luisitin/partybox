# I-134 · Bingo · a late joiner watches the call — shipped option A

Branch `b/I-134-A` applied as is (4 files), plus the Spanish for its two server-written lines
(ADR-044: the line renders through `serverText` with the game's table; the call itself is content).

## Proof

- Capture from this worktree (`LATEJOIN=1`, capture/bingo.ts with its server import pointed at
  session-c — the stock script serves the preview worktree): `captures/bingo/I-134-shipped/`.
- `late-join` clip vs the owner's `I-134-A-late-join`: same screen, the call line bold above
  "BINGO · PLAY" and the scores, which drop under the "more below" arrow as in A's recording; the
  line follows the calls (f040 "B 8 — Figure of eight — skater on the lake", f120 "N 35 — Jump and
  jive — thirty-five").
- `games/bingo/__tests__/spectator.test.ts`: a phone without a card gets `spectator.line`, a player
  gets none. `pnpm verify` green.
