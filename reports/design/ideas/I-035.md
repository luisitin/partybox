# I-035 · The theme sheet is a try-on — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-035.md` (the owner's pick, branch `b/I-035-C`, no note).
Reapplied on `session-c` as the branch has it: the sheet stays open on a tap (Done closes it; the
compact menu still closes itself), each swatch is a tiny screen in the theme's colours, and the
sheet's surface, rows, title and buttons fade to the new colours over `--pb-motion-base`. "Done" is
an i18n string. Noted in docs/DESIGN_SYSTEM.md.

## Proof (2026-09-21, the ideas lobby capture against this worktree)

- `theme-phone.png` — Priya's `theme-phone` clip, the owner's `I-035-C` (top) and mine (bottom):
  the sheet opens over the lobby, Daylight is tapped — the page recolours behind, the sheet fades
  to the light surface while staying up (f45 mid-fade in both) — then Retro Arcade, then Done
  closes it on the recoloured lobby. Frame for frame.
- Cue log: nothing new (the sheet is silent).
- `pnpm verify` GREEN.
