# I-160 · Blanks · on its side the hand is a list beside the question — shipped option B

Branch `b/I-160-B` cherry-picked cleanly (e80c4638: `ControllerHand.tsx`, `HandFan.tsx`,
`blanks.module.css`); prettier re-indented the new split wrapper. No other change.

## Proof (probe on port 42171, WILD deck, answer phase; Priya on an iPhone 15 on its side, Sam on an

upright iPhone SE)

- On its side (layout 659 × 393): the hand gets `handList`; the question card holds the left
  (x 16, 244 px wide, top 98) and the cards run top to bottom on the right (x 276, 367 px wide,
  53–75 px tall, every word visible, no fan dots). After four wheel steps the cards moved up 651 px
  and the question stayed at top 98. Tapping the third card dropped it into the sentence on the
  left ("Zookeeper fired for Elmo asking how you are, and meaning it with the dolphins."), the card
  ringed with its ✓. No horizontal scroll (scrollWidth = innerWidth).
- Upright SE (320 × 568): the fan is unchanged — no `handList`, the 76 %-wide turned cards and the
  "1 of 10" dots as before.
