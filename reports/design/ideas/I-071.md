# I-071 · The host bar names the VIP — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-071.md` (the owner's pick, branch `b/I-071-B`, no note).
Cherry-picked onto `session-c` (a CSS tail conflict with I-053, both kept): the "HOST" label is
the roster's ★ pill (A) carrying the VIP's name (B).

## Proof (2026-09-22, `hostpill-probe.ts` against this worktree)

- Empty room: `★ Host`; with Sam in: `★ Sam` — the pill in the roster's accent-2 fill
  (`I-071/hostbar.png`).
- `pnpm verify` GREEN.
