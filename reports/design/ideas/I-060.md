# I-060 · The empty name preview breathes — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-060.md` (the owner's pick, branch `b/I-060-B`, no note).
Cherry-picked onto `session-c` (conflicts with I-046 / I-047 in the join files, both kept): the
portrait's three dots pulse in turn like a typing indicator until a name is typed; still under
reduced motion, and the `data-motion='off'` twin added.

## Proof (2026-09-22, `dots-probe.ts` against this worktree)

- Three dots; their computed opacities every 100 ms over 1.3 s run in turn —
  `0.36/0.35/0.35 → 1.00/0.63/0.35 → 0.35/0.80/0.96 → 0.35/0.35/0.35` — a 1.2 s loop, 200 ms
  apart.
- Under `prefers-reduced-motion: reduce`: `animation-name: none`. After typing: no dots.
- `pnpm verify` GREEN.
