# I-143 · Blanks · the reading skips bots — shipped option C

Branch `b/I-143-C` applied (8 files, with `takereading.test.ts`). Re-merged onto main: the phone's
read-along caption keeps its Spanish helper; "I'll read" and "the TV reads this one" in Spanish.

Not included, per the spec: the fairness change (rotating over the people only — Session B: "ask the
owner before adding it"; Session C does not ask, so it waits for a spec), and the spoken reader,
which is READER-VOICES.

## Proof (probe on this worktree: TV + Sam + Priya + 3 bots, vote mode; stills in `captures/blanks/I-143-shipped/`)

- A: round 1's reader is Priya, a person: the TV pill "Priya, read it out loud".
- C: Priya's phone drops → Sam's phone shows "I'll read" (screenshot), the TV pill the plain "Read it
  out loud"; Sam taps → reader Sam, the button gone, the pill "Sam, read it out loud".
- B: Sam drops too (no person connected) → the TV pill "the TV reads this one".
- Blanks 150 tests incl. takereading; `pnpm verify` green.
