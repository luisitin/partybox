# I-773 · Blanks · a judge who goes hands the round to the room — shipped option B

Branch `b/I-773-B` cherry-picked cleanly (105e24ad). Session B's binding notes applied: the new
optional `gone?: 'left' | 'kicked'` on the engine's `player` event is recorded as ADR-046
(`docs/DECISIONS.md`) and in `docs/GAME_CONTRACT.md`; the flip was checked with a human judge
(Priya kicked from Sam's ★ menu while she judged).

One fix on top: when the room-judged round was the LAST one, the branch left `settings.judge` on
`vote` into `final` / `done`, so the awards would have switched to vote-mode rules (a Crowd
favourite, no "Read the room", "· n votes" instead of "round n"). `afterResult` now puts the judge
back as the result ends (startRound's own reset is kept); `judge-gone.test.ts` gained that case.
The README's edge-case bullet now describes the flip, and its stale "`reveal` → `judge` (rest of
the reading skipped)" skip line follows I-774.

## Proof (probe on port 42171; czar mode, timed, 6 rounds; Sam VIP + Priya on iPhone SEs, 3 bots)

- Round 2, Priya judging ("Priya is choosing…"): Sam taps Kick twice in the ★ menu. The round
  flipped to a vote 49 ms after the confirming tap — `judgeGone {name: Priya, why: kicked}`, a fresh
  30 s window; the TV reads "ROUND 2 · VOTE" and "Priya was removed — everyone votes this one ·
  0 / 4"; Sam's phone shows "Vote for the best" with the cards. The round's result: Bot 3 wins.
- Round 3, Bot 2 judging, dropped via the dev API (not removed): "Bot 2 dropped — a moment for them
  to come back…" for the grace, then after 21 s "ROUND 3 · VOTE · Bot 2 dropped — everyone votes
  this one · 0 / 3" (`why: dropped`).
- Rounds 3, 4, 5 each open with a judge again (`judge=czar`: Bot 2, Sam, Bot 1).
