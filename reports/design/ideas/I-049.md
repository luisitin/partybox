# I-049 · The room code is one badge — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-049.md` (the owner's pick, branch `b/I-049-C`, no note).
Cherry-picked onto `session-c` as the branch has it: one rounded mono chip with a small ROOM label
in the header and on the QR card (A), the card's letters landing 80 ms apart when the lobby opens
(B), the header's badge bumping on each join (C). Added the letters' reduced-motion /
`data-motion='off'` twins (a literal stagger).

## Proof (2026-09-21, the ideas lobby capture against this worktree)

- `I-049/lobby.png` — the `lobby` clip, the owner's `I-049-C` (rows 1 and 3) and mine (rows 2
  and 4): the TV connecting, the letters landing one by one, the badge in both places, the room
  filling. Frame for frame (the header ring on Sam is I-045's, shipped since the owner's clip).
- Cue log: `ready`, then a `join` per player, as before; nothing new.
- `pnpm verify` GREEN.
