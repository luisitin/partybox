# I-394 · Bingo · the verdict and its reds stay together — shipped option B + the owner's note

`b/I-394-B` (93a2a827) is one constant: the wipe moves from 1.2 s after the verdict to the last beat
before the 3 · 2 · 1. The owner's binding note doubles a wrong claim's read, so this ships as:

- `WRONG_READ_MS = 6_000` (reveal.ts), used by the check phase's verdict tick — its own constant:
  `VERDICT_READ_MS` (3 s) stays for a right claim's read, which I-400's no-pick clock builds on.
- `WIPE_AT_MS = WRONG_READ_MS - 400` = 5.6 s (the branch's 2.6 s was its 3 s read minus 400 ms).
- `keepgoing.test.ts` pins the wrong claim's deadline to `WRONG_READ_MS`. README's check row updated.

Not shipped: ghost reds (option A, `b/I-394-A`). The spec's "B is A + B" line contradicts both its
own finding ("a third would only combine them") and the picked branch, which has no ghost reds —
the branch was followed.

## Proof (probe on this worktree, port 42181; KEEPDAUBS scene: 25 calls skipped, Priya daubs 3 right + 1 never called)

TV trace, seconds from the claim: verdict text "NOT A BINGO · Middle row: 14 was never called · 26,
57 and 1 more were missed" and `judged` at **6.4 s**; the four daubs lift (`wipe` on 4 cells) at
**12.0 s** — 5.6 s after the verdict; play resumes at **12.3 s**. The verdict still screenshot shows
the red ✕ on 14, the green ✓ on 5 and FREE and the dashed "missed" outlines, all on the card for the
whole read (main wiped them 1.2 s in).
