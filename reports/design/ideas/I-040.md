# I-040 · A taken name is the room's business — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-040.md` (the owner's pick, branch `b/I-040-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the engine raises a `to: 'tvs'` toast naming
the clash (A), the toast carries the existing player's id and the lobby rings their chip while it
shows — the I-029 welcome ring skips an active chip (B), the phone's error carries their face and
name with a double buzz (C). Contract: ADR-038 (`toast.to: 'tvs'`, `toast.playerId`,
`error.player`, all additive); the engine test pins the effects.

## Proof (2026-09-21, the ideas lobby capture with JOINFAIL=1 against this worktree)

- `I-040/join-taken.png` — the `join-taken-tv` clip (rows 1–2: the owner's `I-040-C`, mine) and
  `join-taken-phone` (rows 3–4): Priya types "Sam"; the TV's toast "Someone's trying to join as
  Sam — that name's taken" with Sam's chip ringed; the phone's "That name is taken — Sam is
  already in." with Sam's face, the name handed back selected. Frame for frame.
- Cue log at the mark: `error` on Priya's phone at 1.92 s in both; the TV silent, as the branch.
- `pnpm verify` GREEN.
