# I-435 · Bingo · a wrong BINGO! keeps what was right — shipped option A

Cherry-picked `b/I-435-A` (202d2a3f); `Tv.tsx`'s import lines conflicted with I-400's
`NoPickClock` (kept both). `enterCheck` drops the never-called daubs and the claimed line's daubs
(`claim.wiped` names them), every other daub stays; the TV lifts only those (`Card wipeOnly`) and
reads "Wrong daubs and that line wiped. Next number in a moment…"; the phone's note says "Card 1:
the wrong daubs and that line are wiped — the rest stay."; a card left with nothing still reads
"Card wiped" (`wipeKind`). The manifest description and its Spanish say what a wrong claim costs.

On top: `wipe-line.test.ts` (the wrong daub and the line go, right daubs off the line stay,
`claim.wiped` lists exactly what went); the phone's `.wipeNote` is `flex-shrink: 0` — the longer
four-line note was squeezed to its two-line min-height and drew over the B I N G O header in the
first probe; README's overview, check row and claim rule updated. The claimed line is I-392's (the
most daubed), so "that line" is the one the player bet on.

## Proof (probe on this worktree, port 42181; KEEPDAUBS scene: 25 calls skipped, Priya daubs three called numbers in different rows and columns + one never called, then claims)

Claim `cells` = the middle row [10…14], `red` = [0]. TV: "NOT A BINGO · Middle row: 14 was never
called · 26, 57 and 1 more were missed · Wrong daubs and that line wiped" at the verdict (6.4 s);
at 12.0 s exactly **2** cells lift (14 and the row's 5) — 27 and 41 stay; server after the check:
Priya's daubs `[1, 7]`. Phone at the verdict: the four-line note sits above the card, no overlap.
Spanish run: "Marcas equivocadas y esa línea borradas…" on the TV, "Cartón 1: se borran las marcas
equivocadas y esa línea; el resto se queda." on the phone.
