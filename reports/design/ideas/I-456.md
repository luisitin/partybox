# I-456 · Results at 12 players: the arrow clears the buttons, your row in the middle, awards on top — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-456.md` (the owner's pick, branch `b/I-456-C`). Cherry-picked
clean onto `c/lobby-b` (commit `99a5d12b`). One change to the code: the footer-measuring effect
depended on `[footer !== undefined]`, which `react-hooks/exhaustive-deps` rejects as a complex
expression (lint is max-warnings 0); it now depends on `hasFooter = Boolean(footer)` — the same
condition the footer div renders on. `docs/DESIGN_SYSTEM.md` notes A, B and C in the `Screen` line.

## Proof (2026-09-24, port 42201, two iPhone SE phones + 10 bots, Lightning 5 questions)

DOM rects on the final results (viewport 568 px tall):

- Sam, the VIP, 1st with 5,700: footer (Play again / New game / Back to lobby) top at 436; the ▾
  pill at 388–428 — 8 px above the footer, off the button. Sam's row is first, so it sits at the
  top (208–258) where the list starts.
- Priya, a guest, 6th with 550: scroller 188–519 (centre 353.5), her row 328–378 (centre 353) —
  in the middle. ▾ at 471–511, 8 px above "Waiting for Sam…" (footer top 519).
- The title block on both phones: winner line, "You finished 6th · 550 pts", then two award chips
  ("Lightning fingers Bot 7", "Hot streak Sam") — nothing clipped at 320 px width.
- Spanish (phones reloaded with `partybox:lang=es`): "¡Gana Sam!", "Quedaste 6.º · 550 pts",
  "Dedos relámpago", "Racha de fuego", "Jugar otra vez / Otro juego / Ir a la sala"; same positions.
- A last-place player (earlier run, Priya 5th of a tied tail) stops at the bottom with no arrow.

Not run: the ideas `capture/blanks.ts` recording (port 42145 and the ideas captures folder belong
to other sessions).
