# I-787 · lobby · each TV shows its own room's QR — shipped option A

Branch `b/I-787-A` cherry-picked; both conflicts were with I-785 C (shipped just before), which
had added the same `?room=` pass-through in `net/info.ts` and the `asked` code in `/api/info`. Kept
one pass-through and one `asked`, with I-785's private-room filter and I-787's QR room both reading
it. docs/DEV_API.md and the server README say what `qrUrl` now follows.

## Proof (probe on port 42191; QR decoded from the screenshot pixels with OpenCV)

| screen                              | room pill | QR decodes to                          |
| ----------------------------------- | --------- | -------------------------------------- |
| house TV at `/tv`                   | WXTM      | `http://192.168.4.87:42191/?room=WXTM` |
| second room's TV at `/tv?room=TVBX` | TVBX      | `http://192.168.4.87:42191/?room=TVBX` |

An iPhone SE following the second TV's QR link joins TVBX (lands in its lobby as VIP). Before this,
the second TV's QR decoded to the house room. `room-qr.test.ts` pins both cases; `pnpm verify` green.
