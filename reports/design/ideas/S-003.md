# S-003 · Set up your phone in the lobby — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\S-003.md` (the owner's pick, branch `b/S-003-B`, no note).
Cherry-picked onto `session-c` (four files had moved under I-074 / S-002; merged by hand): a
client-module hook `PhoneSettings` the 🎨 sheet renders under each game's name — Bingo's card
style and Motion, the same per-phone stores as the in-game sheet (A); a "🎨 Set up your phone
while you wait" pill under the lobby's waiting line (B). The hook is in docs/GAME_CONTRACT.md.

## Proof (2026-09-21, the ideas lobby capture against this worktree)

- `S-003/theme-phone.png` — the `theme-phone` clip, the owner's `S-003-B` (top) and mine
  (bottom): the pill in the lobby, the sheet with the BINGO section (Focus … Motion) under the
  phone's sound rows, the theme walk, the game starting. The same beats.
- `pnpm verify` GREEN.
