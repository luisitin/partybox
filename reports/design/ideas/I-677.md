# I-677 · The wrong page on the wrong screen offers the other one, and remembers — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-677.md` (the owner's pick, branch `b/I-677-C`). Cherry-picked
clean onto `c/lobby-b` (commit `09785118`). Changes on top:

- **Spanish.** Every sentence of both cards was an English literal; they now go through
  `useT(STRINGS)` with a Spanish table (`surface/strings.ts`), and `scripts/i18n-coverage.test.ts`
  gains the area so a new sentence without Spanish fails the suite.
- The three files moved into `packages/client/src/surface/` (their own folder, so the coverage
  area is exact); option A's unused banner CSS (`.banner/.bottom/.link/.close`) dropped.
- DESIGN_SYSTEM.md's Primitives gets a shell line for the hint.

## Proof (2026-09-24, port 42201)

- Real TV (1080p, no touch) on /tv: no phone card. An iPhone SE on the join page: no big-screen card.
- 1920×1080 mouse screen on the QR join URL: "📺 Is this the TV?" floats at 730–1190 × 16–261,
  clear of the join column (name field at x 16–424, y 342). "Yes — open the TV view" →
  `/tv?room=DVTT`. Next visit to the join URL: "📺 Opening the TV view… 3 · This screen was the TV
  last time · Open it now · Stay here — I'm playing"; it went to `/tv?room=DVTT` by itself 2.8 s later.
- es-ES screen: "📺 ¿Esta es la TV? … Sí: abrir la vista de TV · No, juego aquí".
- iPhone SE on `/tv?room=DVTT`: a 288 × 310 card at y 129 over the dimmed stage (phone-sized, not
  the TV's 1/5 zoom); "📱 Join as a player" → `/?room=DVTT`. "Keep the TV page on this phone" →
  card gone; after a reload it is not asked again (0 cards). es-ES phone: "📺 Esta es la página de
  la TV … Entrar como jugador · Dejar la página de la TV en este teléfono" — no clipping at 320 px.

Not translated: German/French/Portuguese chosen on the join page's own picker read the cards in
English (the shell's `useT` tables are English + Spanish, as everywhere else).
