# I-015 · Blanks' round card is dealt — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-015.md` (the owner's pick, branch `b/I-015-A`, recorded as
`I-013-A` before the renumbering). Reapplied on `session-c` as the branch has it (the pluck delays
as a named constant beside the intro beats; comments say I-015).

## Proof (2026-09-20, the ideas Blanks capture against this worktree)

- Cue log of the `intro` window: `start` 1.53 s, then `card` at 1.89 / 1.96 / 2.04 s (+0 / +75
  / +150 ms), `phase` 6.51 s — the owner's clip: `card` 1.83 / 1.91 / 1.99, `phase` 6.52.
- `cmp-intro.png` — frames 17–26, mine (top) beside the owner's clip: the game-select screen
  crossfades, the two white cards toss in from below with the deal's tilt around the black one,
  "Round 1 of 3" beneath.
- `pnpm verify` GREEN.
