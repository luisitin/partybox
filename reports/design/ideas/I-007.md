# I-007 · Lightning Round's crowd closes in — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-007.md` (the owner's pick, branch `b/I-007-B`). Reapplied on
`session-c` as the branch has it; the `stripActive` hook it adds shipped with I-017 already. One
note: the faces stack in the order the view lists the players (the view carries no lock-in time),
as the branch's own comment says — the spec's "in lock-in order" is the branch's intent, not what
it does; the count still pops per lock-in.

## Proof (2026-09-20, the ideas Lightning capture against this worktree)

- `cmp-question.png` — the `question` window at frames 10–85, mine (top) beside the owner's
  `I-007-B` clip (bottom): the intro, the question with "0 / 8", then faces stacking on the count
  line as the room locks in while the rings drop from the strip's chips, then the reveal.
- Cue log: `phase`, five `lock`s, `reveal`, `phase` — the shell's, unchanged.
- `pnpm verify` GREEN.
