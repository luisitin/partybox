# I-401 · Bingo · results with awards — shipped option B + the owner's note

Cherry-picked `b/I-401-B` (97e5056c); `strings-es-game.ts` conflicted with I-392's lines (both
appends: kept both). A: `history[]` records `clean` per win, `awards()` hands out **Quick draw**
("Bingo on call 44") and **Clean card**. B: `wrongClaims` per player (counted on entering `check`)
gives **Trigger finger** ("1 wrong BINGO!"). Spanish via the game table (serverText translates the
award titles and descriptions).

The owner's note (only an earned award shows; ties and no trigger give none) changed the branch's
picks, which took the first of a sort (a tie handed the award to whichever came first):

- Quick draw: only when ONE player holds the fewest calls (the same player twice is not a tie).
- Clean card / Trigger finger: `soleTop` — the single player with the top count, at least 1.
- `awards.test.ts` rewritten: earned → shown, tie → none, not triggered → none, per award (10 tests).

## Proof (probe on this worktree, port 42181; FINISH scene)

Priya daubs a never-called number and claims (wrong), Sam then daubs a fully-called line and wins,
the VIP ends the game. TV results: "Sam wins!", the board, and three cards — **Quick draw · Sam ·
Bingo on call 49**, **Clean card · Sam · Won with no stray daubs**, **Trigger finger · Priya · 1
wrong BINGO!**. Spanish run: "Mano rápida · Sam · Bingo en la bola 32", "Cartón limpio · Ganó sin
marcas de más", "Gatillo fácil · Priya · 1 ¡BINGO! equivocado" on the TV and on Priya's phone (the
third award sits under the phone's "more below" chevron, as any long results list).
