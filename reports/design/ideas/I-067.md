# I-067 · The join form sideways is two columns — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-067.md` (the owner's pick, branch `b/I-067-B`, no note).
Cherry-picked onto `session-c` (a CSS tail conflict with I-060, both kept): a short landscape box
gets a 44 px join button (A) and two columns — the portrait and name left of the avatar grid, the
status lines spanning both (B).

## Proof (2026-09-22, `fold-probe.ts` on the harness's landscape iPhone against this worktree)

- `I-067/landscape.png` — 852 × 393: the portrait and field on the left, "PICK AN AVATAR" and
  the faces on the right, the button at the foot.
- `avatar header visible sideways: true`, `join button height: 44`; the I-066 chevron carries the
  rest (`scrollTop 0 → 201`, gone at the end).
- `pnpm verify` GREEN.
