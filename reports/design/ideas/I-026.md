# I-026 · Lightning's wagers are chips on a table — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-026.md` (the owner's pick, branch `b/I-026-C`, with the note
"a custom percentage or points amount as an additional option"). Reapplied on `session-c` as the
branch has it (chips, pot line, step-back, breathing footer, `ChoiceGrid.className`), plus:

- **The custom row** (`client/CustomStake.tsx`): under the four presets, in the grid's footer —
  the presets' row shape (chips sized to the share, the amount, "Custom"), a numeric field, a
  `%` / `pts` switch and Place. A percentage (1–100) becomes points by the presets' own rule on
  the phone (`wagerAmount`); points go as typed. Placing it works like a preset: "n in the pot",
  ✓ your stake, the other rows step back; the grid is disabled so a second tap cannot land.
- **Contract:** `{ type: 'wager', amount }` beside the preset `percent` (one zod member, both
  optional, `amount` wins) — the server clamps to 0…score and rounds down to tens (`clampWager`,
  the whole score allowed). The README's Inputs section documents it; bots keep the presets.
- A custom stake that equals a preset's amount is shown as that preset's (the server only stores
  the amount).

## Proof (2026-09-20, the ideas Lightning capture against this worktree, plus a custom-stake run)

- `wager-phone.png` — the owner's `I-026-C` clip (top) and mine (bottom), f4 … f60: the chips on
  every row, Priya's 240 pick with its chips bumping and "240 in the pot", the others at 60 %.
  Mine adds the Custom row under the presets (the SE scrolls for the last two presets).
- `custom-33pct.png` — Priya types 33 %: the row previews 310 (floor of 33 % of 967 to tens)
  with one chip as she types, Place → "310 in the pot", "✓ your stake", the presets step back.
- `custom-120pts.png` — Sam switches to pts, types 12 (previews 10) then 120, Place → "120 in
  the pot"; the final question's footer reads "Your bet: 120".
- `final-reveal.png` — the TV's final reveal: Sam bet 120 → −120, Priya bet 310 → −310 (the
  exact custom amounts), and the winner crowning from I-025 after it.
- Cue log: `submit` on each placed wager (the shell's, on the status flip); the branch names no
  new sound and there is none.
- `pnpm verify` GREEN (a new unit test covers `clampWager` and the `amount` input).
