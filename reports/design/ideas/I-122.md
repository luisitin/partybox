# I-122 · Bingo · missed calls wait in a tray — shipped option A

Branch `b/I-122-A` applied whole (the feel.ts change that I-131's branch had carried stray, plus the
tray and its ✕). Re-merged onto main: Spanish kept and the tray's new words translated; the tray's
state moved into `useMissedTray` in feel.ts (Controller.tsx is at its line limit); B/C's unused
"tap to remove" list dropped (A never removes a ball).

## Proof

Capture from this worktree (`RECONNECT=1`: Priya's phone offline 13 s):
`captures/bingo/I-122-shipped/`, against the owner's `captures/bingo/I-122-A/`.

- `missed-phone` f040: "Back — you missed [N 35] ✕" under the "I 25" header, same as A's f040.
- f120: the tray closed by ✕, the header on "N 33 · before that N 45 · call 7", same as A's f120.
- Bingo tests 85/85; `pnpm verify` green.
