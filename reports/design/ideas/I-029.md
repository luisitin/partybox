# I-029 · The lobby is alive — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-029.md` (the owner's pick, branch `b/I-029-C`, no note).
Reapplied on `session-c` as the branch has it (the walk-in keyframe on the SDK's `enter` chips, the
welcome ring, the two drifting glows behind the lobby), with the welcome ring on the one easing
(`--pb-ease`, not `ease-out`). Noted in docs/DESIGN_SYSTEM.md beside the PlayerChip paragraph.

## Proof (2026-09-21, the ideas lobby capture against this worktree)

- `lobby.png` — the `lobby` clip, the owner's `I-029-C` clip (top) and mine (bottom): the empty
  room with the pink and gold glows drifting, Sam's chip in, the same frames.
- `walk-in-ring.png` — f38 … f52 around Sam's join: the gold welcome ring on the fresh chip fading
  over the next second, frame for frame with the owner's clip.
- Cue log: `ready`, then `join` ×3 at 3.5 / 12.96 / 14.53 s (the owner's: 3.43 / 12.79 / 14.36) —
  the shell's, nothing new.
- `pnpm verify` GREEN.
