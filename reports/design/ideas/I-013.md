# I-013 · Bingo's card styles are shown, not described — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-013.md` (the owner's pick, branch `b/I-013-C`, with the note
"stress test for visual fit so it does not look wacky"). Reapplied on `session-c` as the branch has
it (`StyleMini.tsx` holds the diagram; Overlays.tsx was at the 300-line cap, so the turn gate and
the reconnect toast moved to `Notices.tsx`), plus the note:

- Every row is ONE line at every width: the row text (`.rowText`) ellipsizes before the diagram
  wraps, under 360 px the hint goes, and the sheet's grid column is `minmax(0, 1fr)` — an `auto`
  track took a nowrap row's full text as its size and the rows ran 60–110 px past the sheet's
  edge (the fit test caught it).
- `packages/e2e/src/design/capture-bingo-style-fit.ts` (in the repo): the sheet at 320 / 360 /
  390 / 430 px × 1 / 2 / 3 / 4 cards — sixteen stills, each checked for no row taller than 56 px
  and no horizontal overflow. **32 / 32**: every row 48 px, the sheet 485 px at every size.
- The bob's motion-off twin added; the bob and the pop keep the branch's timings.

## Proof (2026-09-21, the ideas Bingo capture with CARDS=3 against this worktree)

- `style-phone.png` — Priya's `style-phone` clip, the owner's `I-013-C` (top) and mine (bottom):
  the sheet opens over the three cards with a diagram per row and the live one breathing, Grid is
  tapped — the preview bar with the pick at 1.5 × beside "Grid: like it?" over the real Grid — then
  Confirm and the 3 · 2 · 1. The same beats; where the owner's rows wrapped to three lines and
  Strip / Motion fell below the fold, every row is one line and the whole sheet shows.
- `fit-320-430.png` — the fit test's stills at one and three cards.
- Cue log: the 3 · 2 · 1 ticks after Confirm, as before; nothing new.
- `pnpm verify` GREEN.
