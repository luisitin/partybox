# I-017 · Blanks' reader is pointed at — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-017.md` (the owner's pick, branch `b/I-017-B`). Reapplied on
`session-c` as the branch has it; the module's view cast is the typed `BlanksTvView` rather than an
inline shape. `stripActive` is a new optional hook on `GameClientModule` (client side; the server
contract is untouched) — documented in `docs/GAME_CONTRACT.md`'s client-module example.

## Proof (2026-09-20, the ideas Blanks capture against this worktree)

- Cue log of the `reveal` window: four `card` chimes, nothing new — the same as the owner's clip.
- `cmp-reveal.png` — the strip and the pill at frames 12–76 (cards 1–3), mine (top) beside the
  owner's `I-017-B` clip (bottom): the named seat's chip carries the strip's ring for as long as it
  reads (Bot 1 in this run, Sam in the owner's — the seat rotates), and the pill shows the reader's
  face, popping with every new card.
- `pnpm verify` GREEN.
