# I-433 · Bingo · a bot's BINGO! is the claim — shipped option B

Cherry-picked `b/I-433-B` (2bcb9a7f); `strings-es-game.ts` conflicted with I-392/I-401's lines
(appends: kept all). A: `tapBingo` treats a bot's tap as the claim when nobody holds dibs — no arm,
no "hm?"; behind a person's dibs the bot still queues (`bot-claim.test.ts`). B: the TV's head reads
"Bot 2 calls it — checking" instead of "Bot 2 says BINGO!" on the check and on the win (Spanish
"Bot 2 lo canta: comprobando"). README's bot paragraph updated.

The owner's note: only the dibs window is skipped. Nothing after the claim branches on `bot` — the
same reveal, the same verdict read (a wrong one gets I-394's 6 s and I-435's wipe), and after a
bot's win the room still chooses (the bot strategy never sends `continue` / `next`), with I-400's
no-pick clock.

## Proof (probe on this worktree, port 42181; one `random` bot + Priya and Sam, calls skipped every 1.2 s until the bot claims)

- The bot's claim arrived with **0** samples of `round.arm` held by the bot (polled every 100 ms) —
  straight to `bingo`; the TV head: "Bot 1 calls it — checking".
- Its verdict landed 6.4 s after the claim (the reveal of its card, which had one stray daub);
  the deadline was then **22.99 s** away and the room left for the scoreboard **23.1 s** after the
  verdict — a person's win in the same build (I-400 probe): 22.92–22.97 s and 23.1–23.2 s. Equal
  within a frame.
- Priya's phone after the bot's win showed "Keep going — same pattern", "Keep going — blackout",
  "Next round — fresh cards" (Spanish: "Seguir — misma figura" …): the room decides.
