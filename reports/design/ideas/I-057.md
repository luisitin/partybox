# I-057 · A mid-game joiner sits on the bench — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-057.md` (the owner's pick, branch `b/I-057-C`, no note).
Cherry-picked onto `session-c` as the branch has it (a CSS append conflict with I-050, both kept):
a read-only mini-scoreboard from the view the phone already has (A), "Lightning Round · question"
above it and the leader's row in gold with a 🏆 (B), a total that changes bumping with a quiet
`tally` (C). The "changed" set is state adjusted during render (the branch read a ref in render,
which the react-hooks rule refuses).

## Proof (2026-09-21, the ideas lobby capture with LATE=1 against this worktree)

- `I-057/late-phone.png` — the `late-phone` clip, the owner's `I-057-C` (top) and mine (bottom):
  Priya's phone joining while Lightning runs — the bench under the message, the phase line
  changing reveal → question, the leaders in gold. The same beats.
- Cue log at the mark: a quiet `tally` on Priya's phone as a bot scores — mine 12.79 s, the
  owner's 13.23 s (the bots' answer timing).
- `pnpm verify` GREEN.
