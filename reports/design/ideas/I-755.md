# I-755 · lobby · one seat, one tab — shipped option C

Branch `b/I-755-C` cherry-picked cleanly, plus:

- The "open in another tab" screen was English-only: its title, body and button now come from
  `t.otherTab` with Spanish ("PartyBox está abierto en otra pestaña", "Jugar aquí").
- `net/controller.ts` went over the 300-line cap: the stored login/identity moved to
  `net/session-store.ts` and the BroadcastChannel "who has the seat?" to `net/seat-channel.ts`
  (same behaviour, same 300 ms wait).
- A second socket test covers C (four joins with one login in a row: welcome ×3, then
  `kicked: another_tab`); the safety net's per-login map prunes itself past 500 logins.

## Proof (probe on port 42191, one iPhone SE browser context, two tabs)

| moment                                       | tab 1 (playing)                                                      | tab 2 (new)                                            |
| -------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------ |
| 10 s after tab 2 opens the join link (B)     | 0 new sockets, stays in the lobby                                    | 2 sockets total, "PartyBox is open in another tab"     |
| 5 s after tab 2 taps "Play here instead" (A) | 0 new sockets, shows the other-tab screen, no "Reconnecting…" banner | 1 new socket, in the lobby as Priya                    |
| a third tab with the phone in Spanish        | —                                                                    | "PartyBox está abierto en otra pestaña" + "Jugar aquí" |

The room keeps 2 players and Priya connected throughout (main measured ~330 new sockets per tab in
10 s). On the iPhone SE the title wraps to two lines and the button sits at the bottom, no overlap.
`pnpm verify` green.
