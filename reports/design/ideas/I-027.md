# I-027 · Wisecrack's round board is a climb — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-027.md` (the owner's pick, branch `b/I-027-B`, recorded as
`I-014-B`). Reapplied on `session-c` as the branch has it, with two tidy-ups:

- The mover row classes are `rose` / `fell` (the branch reused `down`, the board-level name of
  the existing `stagger="down"` mode — no rule actually collided, but the same name for two
  things in one module is a trap for the next change).
- A `:root[data-motion='off']` twin drops the 1.2 s wait like the reduced-motion block does.

`Scoreboard` (game-sdk) gains `stagger="climb"` + `climbFrom`; documented in DESIGN_SYSTEM.

## Proof (2026-09-20, the ideas Wisecrack capture against this worktree)

- `cmp-scores.png` — the `scores` window at frames 6–36, mine (top) beside the owner's `I-014-B`
  clip (bottom): rows fade in where they stood before the round with their destination rank
  digits, the deltas land, the totals count up, the slide is mid-way at frame 14 (1.2 s + 300 ms)
  and settled by 18 with the green / red outlines fading after.
- Cue log: `phase` only, on both.
- `pnpm verify` GREEN.
