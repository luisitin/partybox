# I-024 · Broken Pencil's books pass hands — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-024.md` (the owner's pick, branch `b/I-024-C`, with the note
"the books need to pass a lot slower and make a quieter sound"). Reapplied on `session-c` with:

- **450 ms per seat** (was 150), each slide still 600 ms; the done book tile leaves **300 ms after
  the ✓**.
- **Quiet:** the engine's `quiet` only kept a cue from suppressing the phase chime — it never
  lowered the level — so the game-sdk `play(cue)` now takes `{ quiet?, gain? }`, both shell
  wrappers pass it through and the engine scales every note and sample by `gain`. The pluck is
  `play('card', { quiet: true, gain: 0.5 })`.
- The slide and plucks play only when a pass opens (rounds 2..P; round 1 is not a hand-off), and
  only on the phase's opening glyph — a mid-phase 💬 → ✏️ change would otherwise hide the new
  glyph for its seat's wait (up to 3 s at the cap).
- `byName` removed (no other caller) instead of exported unused; the ring uses `--pb-ease`;
  reduced-motion / motion-off twins zero the literal delays.
- The spec asks for a twelve-player clip; the game's `maxPlayers` is 8, so the second clip is the
  cap: eight players (six fast bots).

## Proof (2026-09-20, the ideas Broken Pencil capture against this worktree, with a gain hook)

- `pass-open.png`, `pass-books.png` — frames 19/22/25 and 28/31/34 of the `pass` clip: the owner's
  `I-024-C` clip (top), six players (middle), eight players (bottom). Seat 0's 💬 slides in alone at
  f19, seat 1's at f22 (450 ms later); the fast bots finish within the first second as in the
  owner's clip, their ✓ lands, their book tiles are gone by f28 while the phones' cards keep theirs.
- `pass-last-seats-8p.png` — eight players, the last two seats every 200 ms from f17: Priya (seat 6) slides in at f45–47, 2.7 s after the phase opened; Bot 6's ✓ holds its tile one frame after
  landing (the 300 ms wait) before it leaves.
- Cue log (TV, ms after the `pass` mark) — six players: `card` at +5 (under the phase chime),
  +460, +908, +1359, +1809, +2258; eight: … +2710, +3169. Every pluck's gain ramps are 0.04 /
  0.035 = half of `card`'s 0.08 / 0.07 (a full-gain `card` later in the show reads 0.08 / 0.07 in
  the same log, so the hook sees the difference).
- `pnpm verify` GREEN.
