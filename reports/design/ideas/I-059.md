# I-059 · The join form uses a tablet's width — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-059.md` (the owner's pick, branch `b/I-059-B`, no note).
Cherry-picked onto `session-c` as the branch has it: from 760 px the form keeps a left column and
the spare width is a preview stage with your roster chip, live as you type (A), re-popping on
every change (B); the pop's motion-off twin added.

## Proof (2026-09-22, `tablet-join.ts` on the harness's iPad against this worktree)

- `I-059/tablet.png` — the iPad join page: the form on the left, "HOW THE ROOM SEES YOU" with
  the chip on the right, updated as the name and face are picked (`chip text: HOW THE ROOM SEES
YOU Priya you`).
- On an iPhone SE the stage is not visible (`phone stage visible: false`).
- `pnpm verify` GREEN.
