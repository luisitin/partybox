# I-189 · The picker's minutes follow the rounds and the room — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-189.md` (the owner's pick, branch `b/I-189-B`, Session B's
note: re-check the paces against new recaps, keep the `estimate` values in one place).
Applied cleanly from `b/I-189-B`. A manifest may carry `estimate` {fixedSeconds, perRoundSeconds,
perPlayerPerRoundSeconds, roundsSetting} (schema in shared/contract.ts, passed through the game
summaries); `minutesFor` works the picker's "~N min" out from it, the picked game's settings and
the head count (clamped to the game's player bounds), on the phone's cards and the TV's list and big
panel. Blanks (A), Lightning Round and Broken Pencil (B) give their pace; Bingo and Wisecrack keep
their fixed number.

Session B's note: the host PC's recaps were re-read (C:\dev\partybox\recordings, read-only). Since
the spec only two Blanks recaps were added, both solo test runs (1 person + 2 bots: a 27-minute
untimed session and one that ended at the intro) — not real-room paces, so the numbers stay: Blanks
6 rounds at 3–4 took 5.5 / 6.2 / 7.5 min (model 6), Lightning 10 questions 2.7–3.2 (model 3),
Broken Pencil 3.3–3.5 at 3 and 5.6–5.7 at 4 (model 4 and 5). The values live only in each game's
manifest. Docs: ADDING_A_GAME.md explains `estimate`; one README line per game (Blanks' README is at
the 120-line cap, so its existing timing sentence carries it and its "≈ 15 min" became "≈ 6–12").

## Proof (2026-09-24, probe on port 42211)

- Sam + 3 bots, Blanks picked: TV list and big panel "~6 min"; the phone's cards read Bingo ~15,
  Blanks ~6, Broken Pencil ~5, Lightning Round ~3, Wisecrack ~10.
- The TV's Rounds stepper +4 → `rounds: 10` → Blanks "~10 min".
- Six more bots (10 in the room): Blanks "~13 min", Broken Pencil "~12 min" (capped at its 8), the
  others unchanged; the phone matches the TV.
- `pnpm verify` GREEN (estimate.test.ts: 3 tests).
