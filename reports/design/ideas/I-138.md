# I-138 · Bingo · bots answer their own verdicts — shipped option C

Branch `b/I-138-C` reapplied onto main (Tv.tsx had been restructured for Spanish): `claim.bot` on the
claim view (A), `botLine(claim, kind, L)` in copy.ts — pools keyed to what went wrong, a win pool
(B), the line drawn by a hash of the bot's id (C) — rendered under the verdict on the TV, and (added,
per Session B's note that it be visible on a phone) on the phone-only stage. Spanish for all nine
lines; the bot's name stays in front as written.

## Proof (probe on this worktree; screenshots in `captures/bingo/I-138-shipped/`)

A TV room and a phone-only room, 2 fast bots, 3-second calls. A wrong claim is forced for Bot 1
through the dev event API (a never-called square daubed, then BINGO! twice, stamped with the server
clock); a winning one comes from the fast bots on their own.

- TV, wrong: "NOT A BINGO · 14 was never called · … / Bot 1: my sensors were dirty / Card wiped…".
- TV, win: "Bot 1 wins round 1 / Bot 1: beep. gloat." (an earlier run, other ids: "humans: 0").
- Phone-only stage, wrong: "Bot 1: recalculating…". At a bot's win the VIP's phone shows its
  keep-going choice (unchanged), so the win line there is on the other phones' stage.
- `copy.test.ts`: a person gets no line; the same bot gets the same line each time; Spanish.
  `pnpm verify` green.
