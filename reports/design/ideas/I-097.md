# I-097 · The BINGO! button pauses with the room — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-097.md` (the owner's pick, branch `b/I-097-C`, no note).
Cherry-picked onto `session-c` as the branch has it (a CSS tail conflict with I-013, both kept):
the button dims to 0.45 and is disabled while paused (A), reads "⏸ Paused" (B), pops back the
moment play resumes (C); the pop's motion-off twin added.

## Proof (2026-09-21, the ideas lobby capture's pause step against this worktree)

- `I-097/pause-phone.png` — the `pause-phone` clip, the owner's `I-097-C` (top) and mine
  (bottom): BINGO! bright, Sam pauses — the banner and the dimmed "⏸ Paused" — the resume
  countdown, BINGO! back. The same beats (my hold starts a frame earlier; the pause tap's timing).
- Cue log: the resume's three ticks, as before; nothing new.
- `pnpm verify` GREEN.
