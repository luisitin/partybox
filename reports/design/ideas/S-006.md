# S-006 · Broken Pencil · the recap no longer cuts words — shipped option B

Branch `b/S-006-B` applied as is (Tv.module.css only).

## Proof

- Captures from this worktree (capture/pencil.ts pointed at session-c): `captures/pencil/S-006-shipped-longnames/`
  and `S-006-shipped-ownerroom/`.
- `summary` f130 vs the owner's `S-006-B-longnames` / `S-006-B-ownerroom` f130: the same card grid
  (three across, two rows for six books; one row for three), name and ✕ broken on the first line,
  the chain full width below and wrapping at a word ("a dominatrix walking a man → / key"). Only the
  randomly drawn words differ.
- `pnpm verify` green.
