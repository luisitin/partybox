# I-144 · Blanks · a chip per vote — shipped option A

Branch `b/I-144-A` cherry-picked clean (`TvJudge.tsx` `VoteChips` + its CSS). One addition: the
list's screen-reader label goes through `L('votes in')` with Spanish ("votos emitidos"), since the
i18n rule covers every player-facing string. README judge row names it (I-144 A).

## Proof (probe on port 42161: TV 1080p + Sam + Priya on iPhone SE + 3 idle bots, vote mode, one
## vote dealt at a time through the dev API)

- 0 votes: no chip list at all; the pill reads "Vote on your phone · 0 / 5".
- After each vote one 48 px chip joins a centred row at y 898–946, under the grid (grid bottom 862):
  1 chip, then 2, 3, 4 (x 846 / 906 / 966 / 1026 at four — 60 px pitch), each the voter's face;
  the pill keeps its named holdouts ("Just waiting for Bot 3…"), which option A keeps by design.
- The grid moves up ~42 px once the first chip row appears (the stage re-centres the taller block);
  that is the branch's layout, left as picked.
- `pnpm verify` green (653 tests, sim smoke 50 × 6 games, build, doc drift).
