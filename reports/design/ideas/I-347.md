# I-347 · The VIP comes back: no handover outside a game, "Take it back" mid-game — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-347.md` (the owner's pick, branch `b/I-347-C`). Cherry-picked
onto `c/lobby-b` (commit `5fc94969`); three conflicts with I-652 (both add an optional field to
`RoomState`, the snapshot and `RoomSnapshot`) resolved by keeping both `tonight` and `formerVip`.
Changes on top:

- **Spanish.** The pill's words were English literals: now `t.reclaim` (i18n-en-phone / es-phone):
  "Priya tomó el VIP mientras no estabas" · "Recuperarlo"; the server's "Sam is the VIP again" toast
  gets a `serverText` pattern ("Sam vuelve a ser el VIP") for phones and the TV.
- **Line cap.** `ControllerShell.tsx` was at 300 code lines: the pill is its own `ReclaimVip.tsx`
  and the haptic patterns moved to `haptics.ts`.
- **The pill can be put away.** It stays until the next game or a role change and, sitting in the
  toast slot, covers the second answer card on an SE for the rest of the game. Like every other
  toast, a tap on its text now hides it on that phone (the button still reclaims). Recorded as a
  deviation.
- Docs: PROTOCOL.md (handover only during a game; the `reclaimVip` action; snapshot fields),
  TESTING.md's manual step.

## Proof (2026-09-24, port 42201, Sam (VIP) + Priya on iPhone SE, 1 bot, server clock frozen)

- Lobby: Sam's phone dropped, clock moved 35 s → VIP still **Sam**; back, no "Take it back".
- Lightning, question 1: Sam dropped, 35 s → VIP **Priya**. Sam's phone back: one pill "Priya took
  over as VIP while you were away" + "Take it back" (button 103 × 32 px at y 365 on the 568 px SE),
  no second toast stacked. Tapping the text hid it.
- Spanish reload: "Priya tomó el VIP mientras no estabas · Recuperarlo"; tap → VIP **Sam**; Sam's
  phone "Sam vuelve a ser el VIP" + ★ VIP; TV and Priya's phone "Sam is the VIP again"; pill gone.
