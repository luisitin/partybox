# I-093 · Three more Bingo patterns (option C)

Branch `b/I-093-C` cherry-picked clean (`manifest.json`, `server/patterns.ts`, `server/types.ts`).
Prettier reflowed the manifest's option objects on the way in; they were put back on one line each
so the diff is only the three new rows, in all five round selects.

## What ships

- **A** — **Picture frame** (the outer ring, 16 squares) and **Postage stamp** (any 2×2 corner
  block — four completions) in the same machinery as the shipped patterns.
- **B** — **The T** (top row + middle column).
- **C** — the stamp's icon lights ONE example block, the way "any line" shows one row, so the demo
  reads as "a block", not "the corners".

The owner's note ("the stamp should be [any 2x2 corner)") arrived truncated with one character lost
in the round trip; it agrees with option C, and the shipped rule is "any 2×2 corner block", worded
that way in the hint: _"Any 2×2 block in a corner of the card."_

## Proof

Scratchpad `c-capture/probe_I093.ts` runs the shipped checker over each new pattern:

```
frame  completions 1, sizes 16      #####/#...#/#...#/#...#/#####
stamp  completions 4, sizes 4/4/4/4  ##.../##.../...../...../.....   (icon = one block)
tee    completions 1, sizes 9        #####/..#../..#../..#../..#..
every completion wins: true | a completion minus one square wins: false   (all three)
stamp blocks: [[0,1,5,6],[3,4,8,9],[15,16,20,21],[18,19,23,24]]
```

Each pattern's shape is what its name says, the four stamp blocks are the four corners, the icon
shows one of them, and `looksComplete` accepts every listed completion while rejecting each one
short a square. `pnpm verify` GREEN (Bingo's own tests included).
