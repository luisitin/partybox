# I-034 · The last recap is reachable from the room — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-034.md` (the owner's pick, branch `b/I-034-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the results stage says "📼 Recap saved on the
host PC" and the server exposes `GET /api/recaps/latest` (A); the game picker links to it —
`/api/info` carries `lastRecap` (B); `/api/recaps/latest/page` renders the markdown as a page with
the drawings inline, served from `/api/recaps/latest/files/:file` (C). Noted in docs/DEV_API.md.

## Proof (2026-09-22, B's own probe `capture/probe_I034.ts`, recordings ON)

- `status results recording true results? true`; the TV's "Recap saved" line shown;
  `/api/recaps/latest 200 ["dir","gameId","code","files","markdown"]`;
  `/api/recaps/latest/page 200` with the HTML page; the phone's picker shows the link (1 match).
- `pnpm verify` GREEN.
