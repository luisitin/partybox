# R2-01 · One away — the TV knows who is a square from the pattern — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\R2-01.md` (Session B's round-2 proposal, the owner's pick C with
the note "add it as a toggle in the game selection menu rather than a default"). Cherry-picked
`b/R2-01-C` onto `session-c` (one CSS append conflict with I-014's lamps, both kept), plus the note:

- `showClose` (label "One away") in `games/bingo/manifest.json`, off by default, read like
  `showBoard` into `Settings`; the view's `closeIds` is `[]` when it is off, so the strip ring, the
  caption and the `close` cue all stay silent. The phone's own one-to-go pulse is untouched.
- The contract test's settings variant turns it on; the README's settings table and player section
  carry it.
- The `close` effect keys on the joined ids (exhaustive-deps).

## Proof (2026-09-21, the ideas Bingo capture twice, `CLOSE=1` for the second)

- `R2-01/one-away.png` — the `one-away` clip, rows: the owner's `R2-01-C`, mine with the toggle
  on, `r2-main`, mine with the toggle off. On: Sam ringed in the strip and "Sam is one away" under
  the nickname the moment the fourth daub lands, gone with the fifth — the owner's beats. Off:
  identical to main.
- Cue log at the mark: on, the TV plays `close` (392→494 triangle + 587 sine; cut.py labels it
  `call`) at +0.01 s, the owner's C at +0.01 s; off, the TV has no cue there. The phone's own
  `close` at +0.02 s in every run.
- `pnpm verify` GREEN.
