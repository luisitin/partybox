# I-062 · The phone's sound row shows its sound — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-062.md` (the owner's pick, branch `b/I-062-C`, no note).
Cherry-picked onto `session-c` as the branch has it: three bars bounce (scaleY, 900 ms) while On
and freeze flat Off (A), switching off plays a short `lock` before the mute lands (B), and every
cue the phone plays kicks the bars for 400 ms (C); the `data-motion='off'` twins added.

## Proof (2026-09-22, `wave-probe.ts` against this worktree)

- The bars' computed scaleY every 150 ms while On: `0.63/0.95/0.97 → 0.32/0.58/0.93 →
0.37/0.32/0.58 → 1.00/0.80/0.43` — three bars 150 ms apart on a 900 ms loop.
- After the tap: `0.20/0.20/0.20` held flat, the row reading "Sounds on this phone · Off".
- `pnpm verify` GREEN.
