# I-008 · Broken Pencil's pages turn — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-008.md` (the owner's pick, branch `b/I-008-A`). Reapplied on
`session-c` exactly as the branch has it (CSS only).

## Proof (2026-09-20, the ideas Broken Pencil capture against this worktree)

- `cmp-show.png` — the first drawing page's turn, frames −1…+6 around its `card` pluck, mine
  (top; a bot's book, so the word page held 5 s) beside the owner's `I-008-A` clip (bottom; a human's
  book, 12 s): the word page, then the drawing pivoting in on its left edge — edge-on at the pluck,
  mid-turn a frame later, flat by +4 — never a blank frame.
- Cue log: `phase`, `lock`, a `card` pluck per page — the shell's, unchanged.
- `pnpm verify` GREEN.
