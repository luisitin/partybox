# I-666 · One job per header element — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-666.md` (the owner's pick, branch `b/I-666-C`, no note).
Applied cleanly from `b/I-666-C` (ControllerShell auto-merged with I-642's menu listener and the
`ShellCountdown` split). The room code in the phone header is a Share button on every screen (A);
once you've joined the 🎨 goes — your face opens the same sheet — and a 320 px phone keeps your
name, cut with an ellipsis at 72 px (B); the link dot shows only while not connected (C). No new
strings (the code's aria-label reuses `t.lobby.room` and `t.share.button`). DESIGN_SYSTEM.md's
theme line now says where the phone's 🎨 went.

## Proof (2026-09-24, probe on port 42211)

- Sam (iPhone 15, VIP) header: `XXDB` 54–120 px, `★ VIP` 233–296, face + "Sam" 308–377; no 🎨,
  no dot.
- Priya (iPhone SE 320 px, Spanish): code, then face + "Priya" 231–304 — the name stays.
  "Maximiliana" on an SE: "Maximili…" (face + name 192–304).
- Priya taps the code → the Share sheet opens ("Sala XXDB", Mensajes / WhatsApp / Correo / Copiar
  el enlace).
- Maximiliana's phone set offline → after 4 s the dot appears (amber, "reconnecting", 168–180 px)
  left of her face; back online it goes.
- Lightning Round running: the countdown row (now `ShellCountdown`) renders — 1 `role=timer`.
- `pnpm verify` GREEN.
