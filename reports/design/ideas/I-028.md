# I-028 · Wisecrack's cozy round — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-028.md` (the owner's pick, branch `b/I-028-A`, no note).
Cherry-picked onto `session-c` as the branch has it: at exactly three connected players the answer
stage's kicker reads "Round 1 of 2 · cozy round, just the three of you". Copy only.

## Proof (2026-09-21, the ideas Wisecrack capture with BOTS=1 against this worktree)

- `I-028/answer-tv.png` — the `answer-tv` clip at 2 s and 6 s, the owner's `I-028-A` (left) and
  mine (right): the same kicker, the same pips and lines.
- Cue log: `phase`, then a `lock` per answer, as before; nothing new.
- `pnpm verify` GREEN.
