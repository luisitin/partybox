# I-045 · "Waiting for the VIP" is a decision the room can see — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-045.md` (the owner's pick, branch `b/I-045-C`, with the note
"the thinking dots should only appear once the VIP clicks to pick a game and it should be over
their portrait (greying out the portrait to fit the dots)"). Reapplied on `session-c` per the note:

- **A** — the lobby rings the VIP's chip (`activeIds`, beside any nudge ring) and keeps its
  caption "Waiting for Sam to pick a game…"; no dots there.
- **B** — `PlayerChip` gains `thinking` (`PlayerChips` `thinkingIds`): three gold dots pulse in
  turn (opacity only, 1.2 s, twins hold them steady) OVER the avatar, centred, the portrait dimmed
  to 0.35 under them. The selecting stage passes the VIP's id.
- **C** — the selecting stage's caption is "Sam is choosing a game…" with Sam's face inline.
- Found on the way (loop-log 909): with Bingo picked the selecting stage's roster sat under the
  host bar at 1080p (the left column scrolled as a whole); the game list now gives instead and
  the buttons are one line, so all five games, the recap toggle and the roster fit.

## Proof (2026-09-21, `thinking-probe.ts` against this worktree)

- `I-045/lobby.png` — five in the room: Sam's chip ringed, "Waiting for Sam to pick a game…",
  0 dots on the page.
- `I-045/selecting.png` + `selecting-chip.png` — Sam tapped "Pick a game": "Sam is choosing a
  game…" with his face, his chip ringed and the three dots over his dimmed portrait (computed
  portrait opacity 0.35; the dots' opacities sampled every 100 ms over 1.2 s run
  0.25 → 1.00 → 0.25 in turn, 200 ms apart).
- `pnpm verify` GREEN.
