# I-032 · The beds tighten as a deadline closes — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-032.md` (the owner's pick, branch `b/I-032-B`, no note).
Cherry-picked onto `session-c` as the branch has it: `BedEngine.setTension(0..1)` fed by the TV
from the view's deadline every 250 ms (the last ten seconds ramp 0 → 1), the bar scheduler
nudging the tempo up to +12 % (A) and a low-pass opening 1.8 → 8 kHz with it (B). Noted in
docs/DESIGN_SYSTEM.md.

## Proof (2026-09-22, B's own probe `capture/probe_I032.ts` against this worktree)

- A 14 s Lightning question, the bed engine read every two seconds:
  `tension 0 → 0.102 → 0.302 → 0.502 → 0.702 → 0.902`, `barLen 2.000 → 1.976 → 1.930 → 1.886 →
1.845 → 1.805 s` — the spec's numbers (2.00 → 1.80, +11 %).
- `pnpm verify` GREEN.
