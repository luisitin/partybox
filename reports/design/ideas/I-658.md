# I-658 · After the TV's "start over", the phones and the QR follow the new room — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-658.md` (the owner's pick, branch `b/I-658-B`). Cherry-picked
onto `c/lobby-b` (commit `46d5972e`); one import conflict in `TvApp.tsx` with I-677 (both imports
kept). Changes on top:

- `net/controller.ts` went past 300 code lines: the address helpers moved to `net/room-param.ts`
  (`replaceRoomParam`, and `followHouseRoom` for the "room not found" path). Same behaviour.
- Prettier on the branch's `host.test.ts`; DESIGN_SYSTEM.md's TvFrame line notes A and B.

## Proof (2026-09-24, port 42201, 1080p TV + Sam and Priya on iPhone SE, joined by the QR address)

- Before: room UTTY, Sam's address `/?room=UTTY`; the TV's QR paths equal `/api/info`'s qrSvg.
- 🏠 clicked twice: new room SETA 108–131 ms later. The TV re-fetched `/api/info` 28 ms after the
  click and at 0.6 s its QR already matched the new `?room=SETA` code (option A alone waits up to
  60 s).
- Both phones back in the new room by themselves (163–771 ms across runs), nobody tapped, Sam still VIP.
- 3 s later both addresses read `/?room=SETA`; Sam's reload came back connected in SETA.
- A fresh phone opening the old QR address `/?room=UTTY` and joining landed in SETA (Sam, Priya,
  Lee), its address rewritten to `/?room=SETA`.
