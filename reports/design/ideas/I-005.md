# I-005 · Blanks result lands one thing at a time — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-005.md` (the owner's pick, branch `b/I-005-C`). Reapplied on
`session-c` as the branch has it, with one fix:

- The `lock` notes played twice: the branch's effect ran on `beat >= BEAT_AUTHORS`, so the run of
  notes fired again on the winner beat — the owner's own `I-005-C` cue log shows it (locks at
  2.08 / 2.21 s, then a lock under the `win` at 2.68 and another at 2.81). Now `beat ===
BEAT_AUTHORS` only: one note per chip as it lands, none under the sting.

## Proof (2026-09-20, the ideas Blanks capture against this worktree)

- Cue log of the result window: `lock` 2.10 / 2.22 / 2.35 s (three voters, 120 ms apart), `win`
  2.71 s, nothing else until the phase chime — the owner's clip: 2.08 / 2.21, then the doubled run.
- `cmp-result.png` — frames 18–40, mine (top) beside the owner's clip (bottom; theirs was a split):
  the voter chips land one by one, the pills file in fewest votes first, then on the winner beat the
  pills dim to 55 % under twice the confetti.
- `pnpm verify` GREEN.
