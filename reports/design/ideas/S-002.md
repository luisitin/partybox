# S-002 · The daub is a choice — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\S-002.md` (the owner's pick, branch `b/S-002-C`, with the note
"For the ring option the number should be centered inside the ring"). Cherry-picked onto
`session-c` (a CSS tail conflict, both kept; the rows split to `DaubRows.tsx` at the 300-line cap)
plus the note:

- The ring is drawn ON the number — a 1.9 em circle around the centred `.number` span — instead
  of the cell's `::after` at `inset: 6%` (an ellipse on any non-square cell, off the number). It
  is concentric by construction on a cell of any shape; the FREE cell rings the same way in gold.

## Proof (2026-09-21, `daub-probe.ts` on a real phone against this worktree)

- `S-002/daubs.png` — the daubed top row in Blot / Stamp / Ring × Mine / Pink / Gold / Green at
  360 px (top three rows) and 390 px (bottom three): twenty-four stills.
- The ring's centre vs the cell's centre on every cell at both widths: `dx 0.0, dy 0.0`, 41.8 px
  round, `border-radius 50%`, 3 px.
- `pnpm verify` GREEN.
