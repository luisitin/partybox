# I-741 · lobby · a borrowed phone gets your seat back — shipped option C

Branch `b/I-741-C` cherry-picked cleanly, plus:

- The button "That’s me — take my seat" was English-only: now `t.join.takeSeat` with Spanish ("Soy
  yo: recuperar mi sitio"). The two toasts ("Welcome back, Priya — picking up where you left off",
  "Priya is back (new phone)") get Spanish patterns in `server-text.ts`.
- A second server test drives C over real sockets: a connected seat answers `name_taken`, the same
  name with `takeOver` gets the seat's player id and a new login, and the frozen phone's old login
  no longer resumes it.

## Proof (probe on port 42191; screenshots in the session's c-capture/lobby-a/I-741)

| case                               | what happened                                                                                                 |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| A: Priya's phone closed            | room marks her disconnected; a spare iPhone SE typing "Priya" reaches the lobby in 429 ms with her player id  |
| B: the messages                    | phone toast "Welcome back, Priya — picking up where you left off"; TV bottom-left "Priya is back (new phone)" |
| C: Leo's phone frozen (still open) | spare phone: "That name is taken — Leo is already in." and the main button reads "That’s me — take my seat"   |
| C on iPhone SE (320×568)           | button at y 476–556, full width, above the fold (two lines, under the existing "more below" arrow as on main) |
| C tapped                           | spare phone in the lobby as Leo (same id, 3 players, no duplicate), "Welcome back, Leo" toast                 |
| C in Spanish                       | "Ese nombre ya está en uso: Leo está en la sala." + "Soy yo: recuperar mi sitio"                              |

Seen, not changed: the frozen phone, if it wakes up, keeps its stale lobby with "⚠ That name is
taken." (its old login is refused), rather than going back to the join form. `pnpm verify` green.
