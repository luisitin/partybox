# I-002 · Wisecrack reveal: the winner is crowned, the loser steps back — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-002.md` (the owner's pick, branch `b/I-002-B`). Reapplied on
`session-c` as the branch has it, plus the `:root[data-motion='off']` twins the design system asks
of every CSS-module reduced-motion block (the winner's `pb-crown` with `both` would otherwise sit
lifted at once under the in-app motion switch; the branch only covered the OS setting).

## Proof (2026-09-20, the ideas Wisecrack capture against this worktree)

- `cmp-reveal.png` — the reveal, mine (top) beside the owner's `I-002-B` clip (bottom), frames
  5/20/35/50/65/80/95: voters pop, authors rise, then on the points beat the winner lifts with the
  gold outline and the loser shrinks and dims.
- The winner card's outline top, traced per frame (gold pixel scan at x = 60): mine 126 → 124 →
  119 → 122 → 122 (frames 30–38), the owner's clip 127 → 119 → 121 → 122 (32–38) — the crown's
  14 px rise settling 6 px up, within one frame.
- `pnpm verify` GREEN.
