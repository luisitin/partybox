# I-037 · A real tie gets its own ceremony — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-037.md` (the owner's pick, branch `b/I-037-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the tied faces share the crown and the lift
with one smaller confetti and the photo-finish dim (A), popping in together a beat after the line
(B), and a new `tie` cue — the win arpeggio landing on a held Csus4, no horn — in place of
`cheer` (C). The faces' pop got its motion twins; `tie` is in the design-system cue table.

## Proof (2026-09-22, the ideas lobby capture with AFTER=1 TIE=1 against this worktree)

- `I-037/after-tv.png` — the `after-tv` clip, the owner's `I-037-C` (top) and mine (bottom): the
  four tied faces beside "It's a tie!" with the sprinkle, then the lobby's "Last up" card naming
  the tie. The same beats (my bots wear I-043's skins, shipped since the owner's clip).
- Cue log: `tie` at 33.1 s in both runs, in place of `cheer`.
- `pnpm verify` GREEN.
