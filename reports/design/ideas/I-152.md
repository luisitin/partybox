# I-152 · Blanks · who taps Next, said the same way everywhere — shipped option C

Branch `b/I-152-C` reapplied by hand: it predates the Spanish tables and the `ControllerPick`
split, so every line conflicted. Behaviour exactly the branch's, through `L()`:

- A — the `timed` setting reads "off = play at your own pace, the VIP taps Next" (the picker's
  Spanish key moved with it: "…a tu ritmo, el VIP toca Siguiente"); the `Settings` type comment
  in `views.ts` said "anyone" too and now agrees.
- B — the result pill: "Next on Sam's phone" / "Sam toca Siguiente" (falls back to "Next on the
  VIP's phone" when nobody holds the role).
- C — the answer phase: "No clock — Sam taps Next when the room is ready." / "Sin reloj: Sam toca
  Siguiente cuando estén todos listos."; the played phone's hint names them too — "…or when you
  tap Next." on the VIP's own phone, "…or when Sam taps Next." on the others (`nextLine()`).

Four new sentences with Spanish in `strings-es.ts`. README Settings names A/B/C.

## Proof

Probe on port 42161, untimed, Sam (VIP) + Priya on iPhone SE + 3 idle bots, EN and ES:

- Sam's played phone: "2 / 5 in · the reading starts when everyone is in, or when you tap Next."
  with "Start the reading now"; Priya's: "…or when Sam taps Next." (ES: "…cuando toques
  Siguiente." / "…cuando Sam toque Siguiente.").
- TV answer, 2 of 5 in: "Sin reloj: Sam toca Siguiente cuando estén todos listos." on one line
  under the holdouts line.
- TV result pill: "Next on Sam's phone" (ES "Sam toca Siguiente"), top right, no clipping.
- A VIP handover mid-game was not driven; the name is read from the pushed view's `vip` on every
  render, so it follows the role. `pnpm verify` green (660 tests).
