# I-048 · "Remove all 13 bots" — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-048.md` (the owner's pick, branch `b/I-048-A`, no note).
Cherry-picked onto `session-c` as the branch has it: from four bots the host bar's button takes
the danger tone and reads "Remove all N bots". The spec's finding fixed on the way: the button
fired every remove in one burst and the socket's rate limit (a bot action costs 5 of 20 tokens a
second) dropped most of them with "Slow down." — the removes now go out 300 ms apart.

## Proof (2026-09-22, `removeall-probe.ts` against this worktree)

- `I-048/hostbar.png` — the host bar with thirteen bots: "✕ Remove all 13 bots" in the danger
  tone.
- One click → 5.5 s later the room holds Sam alone (13 → 0 bots; the shipped burst left 11 of
  16 in the room in the owner's recording).
- `pnpm verify` GREEN.
