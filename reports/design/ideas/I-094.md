# I-094 · Bingo's swap screen says it once — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-094.md` (the owner's pick, branch `b/I-094-B`, no note).
Cherry-picked onto `session-c` as the branch has it: the swap sentence leaves the pattern hint for
good; the count line ("dealing the cards…" → "swap a card, or tap Ready") is the one instruction.

## Proof (2026-09-21, the ideas Bingo capture with CARDS=3 against this worktree)

- `I-094/intro-phone.png` — the `intro-phone` clip, the owner's `I-094-B` (top, Sam's iPhone)
  and mine (bottom, the capture's Priya on an SE — the hint hides under 640 px there, as shipped):
  the pattern hint is the pattern's alone and the count line carries the instruction.
- Cue log: `card` per dealt card, as before; nothing new.
- `pnpm verify` GREEN.
