# I-039 · A beat between phone screens — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-039.md` (the owner's pick, branch `b/I-039-C`, no note).
Cherry-picked onto `session-c` as the branch has it: `CrossfadeSwap` gains `delayMs`, set on the
live host as `--pb-screen-delay` and honoured by the game-sdk `Screen`'s rise — the shell passes
200 ms during a game (A); Lightning's final question carries "· you bet 240" in its kicker (C).
Noted in docs/DESIGN_SYSTEM.md.

## Proof (2026-09-22, the ideas Lightning capture against this worktree)

- `I-039/final-phone.png` — the `final-phone` clip through the wager → final hand-off, the
  owner's `I-039-C` (top) and mine (bottom): the wager page, then one frame of the ghost alone,
  then the question with "FINAL QUESTION · MOTORSPORT & MORE · YOU BET 240". The same beats.
- `pnpm verify` GREEN.
