# I-400 · Bingo · the winner screen moves on when nobody picks — shipped option B

Cherry-picked `b/I-400-B` (96d95f85) cleanly. A: with a choice to make, the bingo phase's deadline
after the verdict is `VERDICT_READ_MS + NO_PICK_MS` (3 s + 20 s) instead of the 5-minute valve, and
the TV's vote line says "… — no pick in 20 s: next round." (or "finish"). B: `NoPickClock` counts
"Next round in 13" / "Finishing in 13" under it until the first vote, when the vote's own clock
takes over. Spanish in strings-es-tv. On top: `keepgoing.test.ts` now pins the deadline to
read + 20 s and checks the timer lands on `scoreboard` (the branch's own test only pinned the
constant); the phase comment and README timer cell updated.

I-394 kept a wrong claim's longer read in its own constant, so this clock still starts 3 s after a
win's verdict.

## Proof (probe on this worktree, port 42181; NOPICK scene: Sam wins round 1 of 3, nobody taps)

Verdict 5.4 s after the claim; the server deadline then 22.9 s away. TV trace from the verdict:
nothing for 3 s (the read), "Next round in 20" at 3.0 s, then 19 … 1 one a second, `scoreboard` at
23.2 s. The still at "13" shows the vote line and the red clock under it inside the frame. Spanish
run: "Otra ronda en 20" … "1", scoreboard at 23.1 s.
