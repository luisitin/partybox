# I-113 · "Previous number" off now hides it on the phones too (option A)

Branch `b/I-113-A` cherry-picked clean — `games/bingo/server/views.ts`, two lines: the phone's view
sends `previous: null` when `showPrevious` is off (the TV keeps its own flag), so the phone's header
reads "call 4" alone.

## Proof

`games/bingo/__tests__/previous.test.ts` drives a real round three calls in:

- setting on → every phone's `previous` equals the TV's;
- setting off → every phone's `previous` is `null`, the TV reports `showPrevious: false`, and the
  current call is untouched.

Negative control: with `views.ts` put back to main, the "off" test fails (the phone still carried the
number); with the branch, both pass. The README's `showPrevious` row says the phones follow it (the
README is at its 120-line cap, so it rides in the existing row). `pnpm verify` GREEN.
