# I-550 A — say the topic before the bet (+ the owner's note: the bet page, redone)

Shipped from `b/I-550-A` (cherry-picked; three conflicts with I-589 / I-288, both sides kept) plus
the owner's binding note on the pick:

> This is good but also fox how the bet page looks. Look at the recording of the phone and you can
> see it is a bit cramped and visually unappealing.

## What ships

- **A** — during `wager` the TV and every phone carry `finalTopic` (category · topic · difficulty,
  optional, additive on both views). TV: "Final question: Food & Drink · Drinks · hard" under
  "Place your wagers"; phone kicker "Final question: Food & Drink · Drinks · Hard".
- **The note** — the owner's recording (Priya, iPhone SE): one preset visible, the rest under the
  ▾, a two-row Custom block holding half the screen. Now:
  - the presets sit two by two: amount over its share with the chip stack beside it; the odd one
    out (the 0 of five, or a 0-point player's lone 0) spans the row on one line;
  - the share caption is "25 %" (the prompt already says "of your N"); the 100 % tile says
    "All in" (Con todo) — the four chips carry the 100 %;
  - the taken stake's ✓ sits in the tile's corner (nothing shifts when it lands);
  - the open Custom row is one line ("Custom" | field | % | Place); it wraps under "Custom" at
    200 % text instead of crushing the field; Place is never narrower than its word;
  - short phones (≤ 600 px tall): the prompt is body size and the head tightens.

## Proof (probe `lr3.tmp.ts`, port 42231; outputs in the scratchpad `c-capture/lightning3/w7`)

Five phones (Sam iPhone 15 VIP, Priya iPhone SE, Pat Pixel 7, Fay Pixel 200 % text, Lan iPhone 15
sideways) + 3 bots, 2 questions + the final. Wager screen, body scrollHeight / clientHeight:

```
            before (this branch, A only)   after
iphone      —                              475 / 475  no ▾, all 5 presets + Custom in view
iphone-se   526 / 388  ▾, 1 preset offscreen 388 / 388  no ▾, all 5 presets + Custom in view
pixel       659 / 659                      659 / 659
font200     837 / 622  ▾                   697 / 586  ▾ (Custom wraps to two lines, not clipped)
landscape   —                              307 / 245  ▾ (sideways, 245 px body)
SE custom row: form 91→293 px inside a 304 px card; Place 74 px (was clipped at 60)
```

TV: the topic line under "Place your wagers" (w7/wager__tv.png). The rules line sitting against
the "5–6" column label is main's own (r2-main-wager frame 60 shows the same).

`pnpm verify` green (one unrelated flake in push-dedupe.test.ts on the first run; 3/3 alone).
