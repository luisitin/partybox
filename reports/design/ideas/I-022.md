# I-022 · Broken Pencil's recap is read in order — option A (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-022.md` (the owner's pick, branch `b/I-022-A`). Reapplied on
`session-c` exactly as the branch has it.

## Proof (2026-09-20, the ideas Broken Pencil capture against this worktree)

- `cmp-summary.png` — the `summary` window at frames 13–21 (the recap's first second), mine
  (top) beside the owner's `I-022-A` clip (bottom): rows rising one after another across the grid,
  each ✓ / ✕ stamping down a beat after its row, the last stamp landing around frame 21 on both.
- Cue log: the last page's `card`, then the summary's `phase` — the shell's, unchanged.
- `pnpm verify` GREEN.
