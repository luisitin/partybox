# S-005 · A "phone only" room — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\S-005.md` (the owner's pick, branch `b/S-005-C`, no note).
Cherry-picked onto `session-c` — eleven files had moved under S-003 / S-004 (both sides kept,
three seams by hand): the `phoneOnly` room flag + `setPhoneOnly` VIP action on the picker, the
`PhoneStage` / `phoneStagePhases` client-module hook, Bingo's check on every phone (A); the
caller's voice on the phones through the shell's `clip` / `hush` (B); the TV's phase cue on
phones with "TV sounds on this phone" on (C). ADR-041. The call-voice effect keys on the call
stamp with what-to-say in a ref written by an earlier effect (the react-hooks rules); two hooks
(`useMissedCalls`, `useRoundReset`) moved to feel.ts for Controller.tsx's 300-line cap.

## Proof (2026-09-21, the ideas Bingo capture with PHONEONLY=1 against this worktree)

- `S-005/check-other-phone.png` — Sam's phone during Priya's check, the owner's `S-005-C` (top)
  and mine (bottom): "Priya is calling it…", "Priya says BINGO!" with the card turning over cell
  by cell, "NOT A BINGO · 15 and 19 were never called", the 3 · 2 · 1 back to play. The same
  beats.
- Cue log on Sam's phone at the mark: `tick 9.36 · tick 10.38` in both (the resume countdown);
  the caller's voice is a clip the harness does not record — proven by code (`speakCall` on the
  phone's `SoundApi` per call stamp).
- `pnpm verify` GREEN.
