# I-012 · Bingo's next pattern is the size of the board — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-012.md` (the owner's pick, branch `b/I-012-C`, recorded as
`I-023-C` before Session A's renumbering). Reapplied on `session-c` as the branch has it, comments
renamed to I-012.

## Proof (2026-09-20, the ideas Bingo capture against this worktree)

- Cue log of the `next-round` window is identical to the owner's clip: `tally` 1.51 s, then four
  `daub` thumps at 1.56 / 1.67 / 1.81 / 1.96 s (the corners lighting, 140 ms apart, first pass only),
  `phase` 7.52 s, `card` 8.18 s — nothing on the loops after the first.
- `cmp-next.png` — frames 8–70, mine (top) beside the owner's clip (bottom): the BINGO celebration,
  then the Points board with the 216 px demo in its column on the left, "Next: round 2 — Four
  corners" under it, the board keeping its width on the right; the round-2 deal after.
- `pnpm verify` GREEN.
