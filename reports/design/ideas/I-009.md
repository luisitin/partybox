# I-009 · A dropped link is seen and heard — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-009.md` (the owner's pick, branch `b/I-009-C`). Reapplied on
`session-c` as the branch has it, with one guard:

- The return's green ring skips an `.active` chip (`.snap:not(.active)`): the branch's `.snap`
  set `outline: transparent` and animated `outline-color` to transparent with `both` fill on the
  same specificity as `.active`, so a Blanks judge's or a Wisecrack author's gold outline would
  have vanished for good after a reconnect. The pop still plays on an active chip.
- The 30 ms return buzz is a named `BUZZ.back` beside the shell's other patterns.

Shell-wide: every game's TV roster, every phone.

## Proof (2026-09-20, the ideas Wisecrack capture against this worktree)

- Cue log of the `drop` window: TV `leave` at 2.71 s (the cut), `join` when the link returns;
  the phone's own `join` on the same beat — the owner's clip: `leave` 2.71, `join` at its return.
  (The return lands at 8.5 s here and 10.5 s in the owner's clip: the phone's reconnect backoff,
  not the code.)
- Priya's chip brightness, roster band per frame: steady → the flicker over frames 29–34 → a
  ghost until the return → back to full; the owner's clip traces the same shape.
- `cmp-drop-phone.png` — the phone's header, frames 30–90, mine (top) beside the owner's:
  "Reconnecting…" breathing on both, then the timer row back.
- `pnpm verify` GREEN.
