# I-075 · The QR carries the mark — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-075.md` (the owner's pick, branch `b/I-075-C`, no note).
Cherry-picked onto `session-c` as the branch has it: a PB disc at the code's centre, 16 % of its
width, with the server's QR at error level H (A); the disc in the accent fill with a white ring
(B); it bumps on every join (C). The bump's motion twins added.

## Proof (2026-09-22, `qrscan-probe.ts` + OpenCV on the real 1080p TV frames)

- `I-075/qr.png` — the QR card with the mark.
- The code decoded straight off the TV screenshots with `cv2.QRCodeDetector`:
  `tv-empty.png True ('http://192.168.4.87:42355/?room=KGRH',)` and the same for `tv-joined.png`
  (the 560 px empty-room size and the 360 px one) — the mark never breaks a scan.
- `pnpm verify` GREEN.
