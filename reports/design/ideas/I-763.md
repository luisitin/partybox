# I-763 · lobby · settings stay with each game — shipped option C

Branch `b/I-763-C` cherry-picked (one import line in `server/src/app.ts` re-merged next to main's
`attachSpeech`), plus:

- The branch's "Your settings: …" line and "Reset to defaults" link were English-only; they now go
  through `t.selecting` with Spanish ("Tus ajustes: …", "Volver a los de fábrica"), and the setting
  and option labels read through the game's own table (`gameText`). The helper moved to
  `controller/tunedLine.ts`.
- A multiselect default is compared by value (JSON), and a tuned multiselect reads "N ticked"
  rather than its raw comma string.
- `docs/PROTOCOL.md` documents `RoomSnapshot.tuned` and `<recordings>/tuned-settings.json`.

## Proof (probe on port 42191, iPhone SE + iPhone 15)

| step                                    | steppers (Rounds, Cards, Seconds) | card line                                                     |
| --------------------------------------- | --------------------------------- | ------------------------------------------------------------- |
| Bingo picked                            | 3 · 1 · 6                         | none                                                          |
| Cards +3, Seconds −3                    | 3 · 4 · 3                         | —                                                             |
| Blanks tapped                           | 6 · 60 (its own defaults)         | —                                                             |
| Bingo tapped again (A)                  | 3 · 4 · 3                         | "Your settings: Cards per player 4 · Seconds per number 3"    |
| Phone in Spanish                        | same                              | "Tus ajustes: Cartones por jugador 4 · Segundos por número 3" |
| "Volver a los de fábrica" tapped        | 3 · 1 · 6                         | none                                                          |
| Cards +3, then the server restarted (C) | 3 · 4 · 6 on a fresh room/phone   | "Your settings: Cards per player 4"                           |

On the iPhone SE the reset link sits right of "AJUSTES" on the same row (x 160–304, y 260), no
overlap; the tuned line wraps to two lines inside the card. `tuned-settings.json` was written to
the recordings folder with `cards: 4`. Engine tests (`settings-by-game.test.ts`) and
`tuned.test.ts` pass; `pnpm verify` green.
