# I-131 · A claim gets the stage, even in a full room (option C)

Branch `b/I-131-C` cherry-picked **without `games/bingo/client/feel.ts`**: that file's change on the
branch is I-122 A's missed-calls tray (it drops the tray's 5-second self-dismiss and adds a
`useMissedDismiss` hook nothing calls) — not part of this option, and shipped alone it would leave the
missed-calls notice on screen with no way to close it. I-122 was picked tonight and will arrive whole
as its own spec.

## What ships

- **A** — `stripCompact` (client module, phase ids; Bingo: `check`, `bingo`): the strip shows faces only.
- **B** — the claimant's chip (`PlayerChips` `leadId`) keeps its name and is drawn a size up.
- **C** — `stripHidden` (Bingo: `bingo`): no strip on the verdict; the card takes the stage.
- **Session B's note, fixed here:** with the strip gone the card grows, and the _long_ decide line (the
  blackout option, or a winning card sitting the pattern out) ran past the claim stage's bottom — the
  stage clips (`overflow: hidden`) at y 946, the line ran to y 960, "rest play on." cut off. The long
  form now steps down a size (`decideLong`, chosen in `tvBoard.ts` `decideLineClass`); the short form
  keeps its size.

## Proof (scratchpad `c-capture/probe_I131.ts`, TV at 1920×1080, a real room of 13/14 idle bots + Sam + Priya)

Priya's fastest line is called with VIP skips, then she daubs it and claims for real (three cards each,
so the verdict carries the longest decide line):

```
                     before the fix                          after
15 players verdict   line 758-960, clipped by claimStage      line 774-930, not clipped
16 players verdict   line 758-960, clipped by claimStage      line 774-930, not clipped
```

Stills: `review/I-131-15p-verdict.png`, `I-131-16p-verdict.png` — "…the winning card sits the pattern
out; the rest play on." whole, above the sound banner. No strip on the verdict (`chips: 0`).

A wrong claim (the bottom row daubed before any of it is called) → the `check` phase:

```
15 players  13 bot chips "🤖" 163×60 · Sam "★ VIP" · Priya "Priya 0" 275×91
16 players  14 bot chips "🤖" 163×60 · Sam "★ VIP" · Priya "Priya 0" 275×91
```

Faces only, the claimant named and a size up. As the spec says honestly, sixteen faces still wrap to two
rows. `pnpm verify` GREEN.
