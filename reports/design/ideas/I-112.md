# I-112 · Bingo · "Calls shown" replaces two contradictory switches — shipped option C

Branch `b/I-112-C` applied: the generic `impliedBy` hook on a setting spec (A), the settings reader
deriving both flags consistently — board ⇒ previous (B), and one three-way "Calls shown" select
(Memory test (nothing) / Last call only / Full board) in place of "Called board" + "Previous number"
(C). Re-merged onto main: SettingField keeps the Spanish (the implied note reads through the game's
table); the manifest carries only the real change (the branch had reformatted the whole file); the
README settings table has the new row; Spanish for the four new picker lines.

## Proof (probe on this worktree; screenshots in `captures/bingo/I-112-shipped/`)

- The VIP phone's picker and the TV's host panel, Bingo selected, each of the three choices picked
  on the phone: the phone lists the three options, the TV's select follows (`none` / `last` / `board`).
- A game started with each choice, four calls in: `none` — the call only, no "before that", no board;
  `last` — "BEFORE THAT N 34"; `board` — "before that" and the full B–O board (screenshot above).
- `calls-shown.test.ts`: the three choices map to board/previous; the old booleans read consistently.
  `pnpm verify` green.

Note for capture scripts: `showBoard` / `showPrevious` are no longer manifest settings; a dev start
that wants the board passes `callsShown: 'board'` (the ideas capture's `BOARD=1` still sends
`showBoard: true`, which the manifest default `callsShown: 'last'` now overrides).
