# I-004 · Blanks' vote is watched — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-004.md` (the owner's pick, branch `b/I-004-C`). Reapplied on
`session-c` as the branch has it. The `stripActive` hook it adds already shipped with I-017; the
Blanks module's one `stripActive` now answers both phases (judge: whoever has not voted; reveal:
the reader).

## Proof (2026-09-20, the ideas Blanks capture against this worktree)

- Cue log of the `judge` window: `phase` 1.50, `lock` 3.20 / 3.61 / 5.05 (the votes), `tally`
  5.71 (the last vote — "That's everyone"), the result's own `tally` 6.64, `win` 7.84 — the
  owner's clip: locks 2.49 / 4.42 / 5.05, `tally` 5.85 then 6.77, `win` 7.98. (The two `lock`s
  after the result's tally here are I-005's voter chips landing, shipped since the owner's clip.)
- `cmp-judge.png` — frames 10–90, mine (top) beside the owner's `I-004-C` clip (bottom): the
  chips still to vote ringed in the strip and dropping as the votes land, the pill popping with
  each count, the cards bumping on "That's everyone", then the result.
- `pnpm verify` GREEN.
