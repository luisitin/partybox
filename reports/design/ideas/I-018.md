# I-018 · A Rando win is its own beat — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-018.md` (the owner's pick, branch `b/I-018-C`, with the note
"instead of 'the deck' it should still say rando"). Cherry-picked onto `session-c`, plus the note:

- "Rando wins this one! Shame on all of you." and the author line "Rando"; the card-stack mascot
  stays as Rando's face, "+0 · nobody" and "Nobody's score moves." as the branch has them.
- `Voters` and `Author` moved to `TvResultParts.tsx` (TvResult.tsx hit the 300-line cap); the
  wobble's `data-motion='off'` twin added.

## Proof (2026-09-21, the ideas Blanks capture with RANDO=1 against this worktree)

- `I-018/result.png` — the `result` clip, the owner's `I-018-C` (rows 1 and 3) and mine (rows 2
  and 4): the vote grid, the winner card growing with the mascot, "+0 · nobody" and the line under
  the headline. The same beats; "The deck" reads "Rando".
- Cue log at the mark: mine `tally −0.07 · lock 0.53 · lock 0.65 · bust 1.13 · tally 7.94 ·
card 8.81`; the owner's `tally −0.01 · lock 0.60 · 0.72 · 0.84 · bust 1.21 · tally 8.00 ·
card 8.87` (one voter fewer in my round, so one lock fewer).
- `pnpm verify` GREEN.
