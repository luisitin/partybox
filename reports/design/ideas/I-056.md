# I-056 · Three join failures, three treatments — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-056.md` (the owner's pick, branch `b/I-056-C`, no note).
Cherry-picked onto `session-c` as the branch has it: a taken name keeps the shake and the red
field; a full or locked room leaves the name alone — the form dims and the warning wears a badge
(A), the sentence becomes a danger banner above the button (B), the button reads "Room is full" /
"Room is locked" and holds 5 s before a retry (C). The hold is keyed on the error object instead
of a synchronous `setState` in the effect (the react-hooks rule); the banner's motion-off twin
added.

## Proof (2026-09-21, the ideas lobby capture with JOINFAIL=1 against this worktree)

- `I-056/join-fails.png` — the `join-locked-phone` clip (rows 1–2: the owner's `I-056-C`, mine)
  and `join-taken-phone` (rows 3–4): the locked banner over a dimmed form with "Room is locked"
  holding, then "Join" back at 5 s; the taken name shaking red with the name handed back (mine
  with I-040's face, shipped since the owner's clip). The same beats.
- Cue log: one `error` per rejection on Priya's phone, as before.
- `pnpm verify` GREEN.
