# I-073 · The lobby remembers the last game — option A + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-073.md` (the owner's pick, branch `b/I-073-A`, with the note
"Needs to account for ties or no winners"). Reapplied on `session-c` (the lobby files had moved
under I-045/049/072) plus the note:

- `toLobby` keeps `room.results`; the next game's start clears them (ADR-039).
- The "Last up · <game>" card beside the roster: one winner "Bot 2 won"; a tie "Bot 2 and Bot 1
  tied" ("Sam, Priya and Bot 2 tied" from three; faces first, four at most then "+n"); nobody
  scored — every "winner" on zero, as after the VIP ends a game early — "no winner" in the muted
  colour with no face.

## Proof (2026-09-21, `lastup-probe.ts` against this worktree — three Lightning games)

- `I-073/winner.png` — three fast bots, Sam idle: "Bot 2 won" with Bot 2's face.
- `I-073/tie.png` — the same game with the top two scores made equal at the last reveal (the dev
  load-state; speed points make a natural tie near impossible): "Bot 2 and Bot 1 tied", two faces.
- `I-073/ended.png` — the VIP ends the game before anyone scores: "no winner", muted, no face
  (the engine's `winnerIds` lists all four at zero — the card reads the scores).
- `pnpm verify` GREEN (the engine test now pins that `toLobby` keeps the results).
