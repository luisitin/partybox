# I-016 · Blanks' hand is a hand — option C with the owner's legibility note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-016.md` (the owner's pick, branch `b/I-016-C`, recorded as
`I-015-C`). The owner's binding note: every card's whole text must read without a tap. Reapplied
on `session-c` with these changes against the branch:

- No overlap: a card is 76 % of the row (was 64 % with a −7 % overlap on each side) with a small
  gap; neighbours sit beside a card, never under it.
- The turn is ±1° a card (was 1.6°) about the same low pivot; the lift on tap and the flight on
  Play are as the branch has them.
- A card longer than 64 characters steps its type down one size (`--pb-font-caption`) so it
  reads whole at the SE's width — never truncated.
- The flight's send timer is cleared if the screen unmounts first.

## Proof (2026-09-20)

- `se-hand.png`, `se-card-3.png`, `se-cards.png` — the ten longest white cards of the shipped
  decks (WILD included, 72–80 characters) dealt to an iPhone SE via the dev API: every card reads
  whole when scrolled to; the neighbours are cut only by the screen's edge, not by a card.
- `pixel-picked.png` — a Pixel with a card picked: lifted with its ring, the rest at 70 %.
- `cmp-hand.png` — the ideas Blanks capture's `hand-phone` clip, mine (top) beside the owner's
  `I-015-C` clip (bottom): the same deal, tap and Play beats (`submit` at 5.1 s on both); the
  owner's fan shows the overlap and the broken words the note rejected, mine reads whole.
- `pnpm verify` GREEN.
