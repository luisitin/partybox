# I-774 · Blanks · the VIP's Skip / Next during the reading is the next card — shipped option B

Branch `b/I-774-B` cherry-picked cleanly (8e880f20). One deviation: `ControllerShell.tsx` sat at the
300-line cap, so the branch's inline label translation moved into `VipMenu.tsx` as
`vipMenuState(view)` (pause state + the translated `vipSkipLabel`), spread onto the menu — same
behaviour, zero net lines in the shell.

## Proof (probe on port 42171; 5 players, vote mode, Sam's phone in Spanish on an iPhone SE)

Each step taps the TV host bar's ⏭ button once, then reads the TV bar and the phone's ★ menu:

| on card | TV host bar          | phone ★ menu (es)        |
| ------- | -------------------- | ------------------------ |
| 1 / 5   | ⏭ Next card (2 of 5) | Siguiente carta (2 de 5) |
| 2 / 5   | ⏭ Next card (3 of 5) | Siguiente carta (3 de 5) |
| 3 / 5   | ⏭ Next card (4 of 5) | Siguiente carta (4 de 5) |
| 4 / 5   | ⏭ Next card (5 of 5) | Siguiente carta (5 de 5) |
| 5 / 5   | ⏭ Open the vote      | Abrir la votación        |

The fifth tap opens `judge`. "Abrir la votación" wraps to two lines inside its half-width button on
the SE with no clipping; the TV bar fits on one row.
