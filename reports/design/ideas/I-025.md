# I-025 · The winner is crowned — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-025.md` (the owner's pick, branch `b/I-025-C`, no note).
Reapplied on `session-c` as the branch has it, in the shell's `TvResults` (every game's results),
with the photo finish timed as `calc(var(--pb-motion-slow) * 2)` (1.2 s, and it collapses with
the tokens under reduced motion — a literal 1200 ms would not) and the bot flag read from the
results' own player list. Noted in docs/DESIGN_SYSTEM.md (the TV status-swap paragraph).

## Proof (2026-09-20, the ideas Lightning capture against this worktree)

- `winner.png` — frames 27/29/31/34/38 of the `winner` clip, the owner's `I-025-C` clip (top) and
  mine (bottom): the crossfade from the final reveal, the headline with the winner's face and the
  confetti (a bot won both runs — sixteen pieces), the finale board at 55 % under it for two
  frames, then back to full by f38 (1.2 s after the crown).
- `winner-settle.png` — f42 … f120: the board at full, the confetti still falling, nothing else
  moves; the hint line and the VIP buttons as before.
- Cue log: `jackpot` at the reveal (the game's), the shell's horn + cheer for the results — the
  cues the owner's clip has, nothing new.
- `pnpm verify` GREEN.
