# I-646 · The TV shows a second code for friends elsewhere — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-646.md` (the owner's pick, branch `b/I-646-C`, no note).
Applied from `b/I-646-C` (TvLobby.module.css conflicted only with I-650's appended tally block).
`/api/info` carries `publicQrUrl` (the tunnel's join link with the house room) and `publicQrSvg`
while a tunnel is live, nothing without one; the TV's card adds the tunnel's code, small, beside
"🌍 Not on this Wi-Fi? / Scan this one" (Spanish already in the branch). The branch builds only
the C layout (both codes); the spec's "A + B + C" line is its template — no switch was built.

Deviation: in an EMPTY room (the 560 px code, I-072) the second code under the big one pushed the
LAN address and the room code off a 1080p stage (card bottom 1266 px). There the tunnel's code now
sits beside the card (absolute, vertically centred); with anyone in the room it stays under the
Wi-Fi code as the branch has it. New server test for the info fields; ARCHITECTURE.md notes them.

## Proof (2026-09-24, probe on port 42211, `PARTYBOX_PUBLIC_URL=https://party-demo.trycloudflare.com`)

- No tunnel: `publicQrUrl`/`publicQrSvg` null, no second code; the TV unchanged.
- Tunnel, empty room, 1920×1080: second code 150×150 at x=1304 beside the card; ROOM code at
  y=1006 as on main. Decoded from the screenshot (OpenCV): `https://party-demo.trycloudflare.com/?room=HKTS`
  and `http://192.168.4.87:42211/?room=HKTS`.
- Tunnel, 6 people, 1080p and 12 people, 1920×1200: second code under the Wi-Fi code at y=656,
  card bottom 1066 (fits); both codes decode to the right links.
- Tunnel, 1280×720, TV in Spanish: "🌍 ¿No estás en este Wi-Fi? / Escanea este", code 100 px,
  decodes.
- `pnpm verify` GREEN.
