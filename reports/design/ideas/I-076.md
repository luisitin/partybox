# I-076 · The join screen speaks the phone's language — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-076.md` (the owner's pick, branch `b/I-076-C`, no note).
The branch's Join/Lobby predate I-046/059/067/070, so the i18n layer was applied by hand on the
current files: `joinStrings`/`joinLang`/`setJoinLang` and the es/de/fr/pt table from the branch's
`i18n.ts` (A), the EN · ES · DE · FR · PT pill row under the form (B), and the phone lobby's
waiting line and "Add a bot" through `lobbyStrings()` (C).

## Proof (2026-09-22, `lang-probe.ts` against this worktree)

- The join page opened with `?lang=…`: `es → Únete a la fiesta | Escribe un nombre para entrar`,
  `de → Mach mit | Gib einen Namen ein`, `fr → Rejoins la fête | Entre un prénom pour rejoindre`,
  `pt → Entra na festa | Escreve um nome para entrar`.
- Tapping the EN pill: `Join the party` — the choice is remembered in the browser.
- `pnpm verify` GREEN.
