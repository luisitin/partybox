# I-445 · shipped option B (Blanks: bots vote with a person's card in mind, and each bot has its own taste)

Cherry-picked `b/I-445-B` (9217c5dc) onto session-c with no conflicts; Prettier re-wrapped three
lines of the branch (no code change). Server-only: `games/blanks/server/bot.ts` adds
`PERSON_BONUS = 0.2` to a bot's vote for a person's card (not a bot's, not Rando's) and a fixed
per-bot lean `taste(botId, cards)` in ±0.2 (FNV-1a). No view, wire field, string or sound changes,
so no i18n rows; the README's bot paragraph gains one sentence (I-445 A / B).

## Win share (Session B's own simulation, run read-only against this worktree)

One person playing and voting at random plus 2 or 4 bots, vote mode, 400 one-round games a room.
"Before" swaps session-c's `bot.ts` back in; "after" is this branch.

| Room         | Fair share | Before | After  | All bots on one card, before → after |
| ------------ | ---------- | ------ | ------ | ------------------------------------ |
| wild, 2 bots | 33.3 %     | 9.3 %  | 31.8 % | 3.8 % → 19.5 %                       |
| wild, 4 bots | 20 %       | 3.3 %  | 19.8 % | 0.8 % → 5 %                          |
| mild, 2 bots | 33.3 %     | 12.8 % | 33 %   | 6 % → 20.5 %                         |
| mild, 4 bots | 20 %       | 4.3 %  | 22 %   | 0.8 % → 4.3 %                        |

Identical to the spec's figures for B.

## Live probe (port 42251)

`i445-bots-vote.tmp.ts`: Ana on an iPhone + two `fast` server bots, vote mode, wild deck, 3 rounds,
seed 11. Round 1 the bots split (Bot 1 → Bot 2's card, Bot 2 → Bot 1's; Bot 1 won); rounds 2 and 3
both bots voted Ana's card and she won. Server log: no errors. Screenshots of the TV and phone at
judge and result, each round: the vote grid, the voter chips, "Ana wins the round!" with "Voted by
Bot 2 and Bot 1" on her phone — all render as before (the option changes no pixels).

`pnpm verify` green (148 Blanks tests, incl. the new `bot-people.test.ts`).
