# I-099 · The card-pick step sideways is two halves — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-099.md` (the owner's pick, branch `b/I-099-C`, no note).
Cherry-picked onto `session-c` as the branch has it (a CSS tail conflict with I-097, both kept):
two centred columns (A), the pattern demo at 92 px sideways (B), Picked / Ready under the card in
the right half with the portrait footer hidden (C). `useLandscape` lives in `feel.ts` and the
buttons are one element rendered in either slot (Controller.tsx sat at the 300-line cap).

## Proof (2026-09-21, the ideas Bingo capture with LANDSCAPE=1 against this worktree)

- `I-099/intro-landscape.png` — the `intro-landscape` clip, the owner's `I-099-C` (top four)
  and mine (bottom four): the pattern big on the left, the card with Picked / Ready on the right,
  "everyone is ready" → "first number in", then the play screen's upright prompt. The same beats.
- Cue log: `card` for the deal, `submit` on Ready, as before.
- `pnpm verify` GREEN.
