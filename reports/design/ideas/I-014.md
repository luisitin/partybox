# I-014 · Bingo's hall board is lamps — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-014.md` (the owner's pick, branch `b/I-014-C`, with the note
"the flicker needs to be a lot smoother … headlessly tested visually for smoothness and no cuts").
Applied on `session-c` as the branch has it — round 40 px bulbs with a halo per called number (A),
the current one catching as the ball lands with a `tick` 60 ms after (B), numbers that arrive
together lighting 40 ms apart (C) — plus the note:

- The catch is one 320 ms `opacity` + `background-color` fade with a single dip (0.45 → 0.82 →
  0.58 → 1) on `--pb-ease`; every keyframe is a colour or an opacity, no `background` shorthand
  (not animatable across layers: it cut) and no scale overshoot.
- `packages/e2e/src/design/capture-bingo-lamp-smooth.ts` (in the repo): the real game with the
  board on; each new lamp's computed opacity and background colour sampled every frame through the
  catch, no sample-to-sample step over 0.25 opacity / 25 % of the RGB diagonal, ends lit.
  **10 / 10** over three catches: largest opacity step 0.16, largest colour step 19 %.
- Found on the way: the first call's tick was lost — a dev-mode double effect (and any remount)
  cleared the timer while the `seen` ref kept the number. The cleanup now un-sees numbers whose
  tick has not fired; the cue log shows call → tick +50 ms on every call.

## Proof (2026-09-21, the ideas Bingo capture with BOARD=1 against this worktree)

- `I-014/call-lands.png` — the `call-lands` clip, the owner's `I-014-C` (rows 1 and 3) and mine
  (rows 2 and 4): the 3 · 2 · 1, B 9 landing, the lamp lit. The same beats.
- `I-014/lamps.png` — the board at full size: bulbs, the I row's halo on 20 as it catches.
- Cue log (mine): `tick −0.82 · call 0.21 · tick 0.26 · call 6.20 · tick 6.26 · call 12.21 ·
tick 12.26`; the owner's: `call 1.70 · tick 1.76 · call 7.71 · tick 7.77`.
- `pnpm verify` GREEN.
