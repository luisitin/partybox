# I-794 · broken-pencil · design review H: the drawing sheet gets the height — shipped

No Session B branch existed (`b/I-794-A` absent), so this was built from the review's drawn after
(`specs/design-review-f4f43d17/phone-screens-redrawn.html#H`, screenshotted with Playwright).

- Whose book and the round moved into the shell's timer bar, left of the bar:
  "Maximiliano's book · 1/6". New SDK store `useTimerLabel(text, tail)` (game screens) /
  `useShellTimerLabel()` (ShellCountdown); the name gives way with "…", the `· 1/6` tail never does;
  under "Reconnecting…" or the hurry cue the label is hidden in place (visibility), so the bar never
  jumps. The first name only (the owner's "Max's"); a hidden timer keeps the old kicker.
- The prompt is one bold line, the word in the accent: "Draw: “yoga class”" / "Draw your guess: …"
  (Spanish "Dibuja:" / "Dibuja tu respuesta:").
- DrawPad: the sheet first, then the eight colours (spread full width), then one band: a thin ink
  line, the three pen sizes as one pill, Undo, Clear. The ink meter's words show only when out of
  ink / too many strokes. The 60vh cap on the sheet is gone; a tall phone seats the sheet on its
  colours with the slack above. Sideways phones keep the three-column layout (colours left).

## Proof (probe `pencil-fit.tmp.ts` on port 42251; 4 review phones + a Spanish SE + landscape)

Draw screen, round 1 (sheet px · Clear bottom vs footer top):

| phone              | before                                              | after                               |
| ------------------ | --------------------------------------------------- | ----------------------------------- |
| iPhone 15          | 248 px sheet, Clear ends 563 < footer 579           | 305 px, Clear ends 563 < footer 579 |
| iPhone SE          | 180 px sheet, Clear 453–497 under the footer at 488 | 224 px, Clear ends 472 < footer 488 |
| Pixel 7            | 380 px sheet (width-bound)                          | 380 px, Clear ends 743 < footer 759 |
| Pixel 7 · 200 %    | 180 px sheet, Clear ends 697 under footer 679, ▾    | 319 px, Clear ends 663 < footer 679 |
| iPhone SE, Spanish | –                                                   | 224 px, "Deshacer" / "Borrar" whole |

Timer rows
read "Kenji's book · 1/6 | 58 s", "El libro de Lucía · 1/6", and at 200 % text "Pablo's book · 1/6"
whole; "Maximiliano's… · 2/4" there. No horizontal scroll, no "more below" arrow on any draw screen.
`pnpm verify` green.
