# I-047 · The photo swap is a flip — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-047.md` (the owner's pick, branch `b/I-047-B`, no note).
Cherry-picked onto `session-c` (a CSS tail conflict with I-046, both kept): the portrait turns
180° between face and photo, a face-to-face pick keeps its pop (A); a gold ring and "your photo"
while a photo is in (B). Motion-off twins for the flips.

## Proof (2026-09-22, the ideas lobby capture with PHOTO=1 against this worktree)

- `I-047/flip.png` — the `join-phone` clip's portrait through the flip back to a face, the
  owner's `I-047-B` (top) and mine (bottom): the photo in its gold ring with "your photo", the
  edge-on frame of the turn, the face landing. The same beats.
- `pnpm verify` GREEN.
