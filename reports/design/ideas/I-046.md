# I-046 · The name field's example rotates — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-046.md` (the owner's pick, branch `b/I-046-C`, no note).
Cherry-picked onto `session-c` as the branch has it: the placeholder rotates every 2.5 s while the
field is empty and unfocused (A), the room's own people first — `/api/info` `rooms[].names` (B),
a random start per phone (C); the crossfade's motion-off twin added.

## Proof (2026-09-22, `placeholder-probe.ts` against this worktree)

- Priya's phone on the join page with Sam in the room, the placeholder read every 2.5 s:
  `e.g. Sam | e.g. Priya | e.g. Grandma Jo | e.g. Big Dave | e.g. Mo | e.g. Auntie Kay` — the
  room's Sam leads, then the examples; focused, it holds (`e.g. Sam → e.g. Sam` over 5 s).
- `pnpm verify` GREEN.
