# I-126 · Bingo · a tablet keeps the style pill and gets a caller strip — shipped option B

Branch `b/I-126-B` applied (5 files) — B, never C's history row (Session B's note). Re-merged onto
main: the Spanish in CallRow / StyleSheet kept, the new tablet row ("All cards · the tablet layout",
"on ✓" / "tablet") in Spanish. Three Bingo files crossed the 300-line limit, so pieces moved with no
behaviour change: `StyleEntry.tsx` (the 🃏 pill, the card-pick sheet, and the in-round sheet with
the tablet's pick), `IntroParts.tsx` (the intro's count and buttons).

## Proof

Capture from this worktree (`IPAD=1 CARDS=4`, capture/bingo.ts pointed at session-c):
`captures/bingo/I-126-shipped/`, against the owner's `captures/bingo/I-126-B/`.

- `style-phone` contact frame: the same header — "I 25", "Quarter of a hundred — twenty-five",
  "before that N 35 · call 5", the 🃏 style pill — and the same 2 × 2 cards; only the dealt numbers differ.
- f040: the sheet open, "All cards · the tablet layout — on ✓" first, identical row by row.
- f150: the resume 3 · 2 · 1 over the same layout with "Any line · get your thumbs ready".
- `pnpm verify` green.
