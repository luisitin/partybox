# I-187 · Blanks · the deck is on the Start button — shipped option C

Branch `b/I-187-C` cherry-picked cleanly (077621d4), including Session B's binding note: the SDK
Screen's "more below" ▾ pill now hangs from a zero-height anchor on the footer's top edge.
Fixes on top: the branch's English-only literals went through the tables — the phone chip is
`t.selecting.keyChip` ("🔞 WILD deck — change" / "🔞 Mazo WILD — cambiar"), the TV tag is one
template `'{mark} {deck} deck'` → `'{mark} mazo {deck}'` (the branch's `{deck} {L('deck')}` read
"WILD mazo" in Spanish), and Blanks' strings gained `Family night` → `Noche familiar`, `Adults` →
`Adultos` for the short names.

## Proof (probe on port 42171; Sam VIP on an iPhone SE 320 × 568, TV 1920 × 1080; en and es)

- Picked Blanks (default deck): button "Start Blanks · WILD" / "Empezar Blanks · WILD", one line
  (top 448, bottom 504), no text overflow; chip "🔞 WILD deck — change" in the danger red
  (rgb 239, 71, 111) at y 133–165; the ▾ pill at y 388–428 sits 20 px above the Start button (it
  used to cover it). TV meta line: "3–12 players · ~15 min · 🔞 WILD deck" / "3–12 jugadores ·
  ~15 min · 🔞 mazo WILD".
- Tapping the chip scrolls the Decks setting into view (y 248–292 of 568).
- Deck → Family night: "Start Blanks · Family night"; in Spanish "Empezar Blanks · Noche familiar"
  wraps to two lines (top 424) inside the button, no clipping; the chip turns plain (👪, text
  colour); the TV reads "👪 mazo Noche familiar".

## Left for the owner

- Pre-existing, not from this option: on a 320 px phone the picker's select controls do not shrink —
  "WILD (all three decks)" runs ~24 px past the right edge of the Decks card, squeezing the
  description to a one-word column. The chip now leads straight to it. `SettingField.module.css`
  was not touched here.
