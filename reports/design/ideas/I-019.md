# I-019 · "And the winner is…" has an envelope to open — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-019.md` (the owner's pick, branch `b/I-019-C`, with the note
"the envelope needs to work better. It seems bugged visually"). Cherry-picked onto `session-c`
(one CSS append conflict with I-018, both kept), plus the note:

- The envelope is an inline SVG (`viewBox 0 0 28 20`, 1.4 em × 1 em on the h2's baseline): the
  back, the letter, the front pocket and the flap, each a module class. The flap turns open on its
  top edge (`rotateX(-150deg)`, `--pb-motion-slow`, `--pb-ease`) at 1600 ms — exactly as the hold
  ends — and the letter rises 8 units (≈ 12 px at the h2 size) at 2200 ms over `--pb-motion-base`.
  Transform only; the reduced-motion and `data-motion='off'` twins leave it open at rest.
- The crown's literal 2200 ms delay got its twins; `holdMs` is noted in docs/DESIGN_SYSTEM.md.
- First cut used bare class names inside the module — the parts rendered black (CSS modules hash
  every class); module classes fixed it.

## Proof (2026-09-21, the ideas Blanks capture with FINAL=1 against this worktree)

- `I-019/envelope-stills.png` — the envelope at 0 / 300 / 600 ms after the hold ends and at
  rest (1400 ms), night (top) and daylight (bottom): closed, the flap turning, open with the
  letter rising, at rest.
- `I-019/final.png` — the `final` clip, the owner's `I-019-C` (rows 1 and 3) and mine (rows 2
  and 4): the board holds "—" and "·", the envelope opens, the totals count, the top row lifts.
- Cue log at the mark: mine `tally −0.41 · tick 0.23 · card 0.33 · tick 0.45 · 0.64 · 0.80 ·
0.93 · 1.04 · 1.13 · 1.20 · fanfare 1.81`; the owner's `tally −0.40 · tick 0.25 · card 0.33 ·
tick 0.47 · 0.65 · 0.81 · 0.94 · 1.05 · 1.14 · 1.21 · fanfare 1.81`.
- `pnpm verify` GREEN.
