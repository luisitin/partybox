# I-392 · Bingo · a wrong claim is judged on the line it bet on — shipped option B

Cherry-picked `b/I-392-B` (0c47d6cf) cleanly. A: `evaluate` picks the completion with the most
**daubed** cells, ties to the most called (by called cells alone FREE made the untouched middle row
"the best completion" of a daubed top row). B: `whyNot` leads with the line's name (`lineName`:
Top row / Row 2 / Middle row / Row 4 / Bottom row / The B…O column / The diagonal; null for a
pattern that is not one line), Spanish in strings-es-game. Tests: `claim-line.test.ts` (the line
choice, a real bingo unchanged, plus added `lineName` rows/columns/diagonal/corners and the led
reason). README's claim rule updated.

## Proof (probe on this worktree, port 42181; TV 1080p + Priya on iPhone SE)

Priya daubs her whole top row on call 1 (11 22 31 57 75, none called) and claims:

- server: claim `cells` [0, 1, 2, 3, 4], `red` [0, 1, 2, 3, 4] — the top row, no bogus "missed"
  middle-row numbers.
- TV at 2.7 s (the verdict): "Top row: 11, 22 and 3 more were never called", five red ✕ on the
  top row; `judged` at 2.9 s, the wipe at 4.0 s, play again at 5.9 s (the hopeless fast path, I-117).
- Phone: "Top row: 11, 22 and 3 more were never called. Card 1 wiped — re-daub from memory…".
- Spanish run: "Fila de arriba: 11, 22 y 3 más nunca salieron".
