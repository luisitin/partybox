# I-149 · Blanks · judge mode's side bet — shipped option C

Branch `b/I-149-C` applied (10 files, with `sidebet.test.ts`). Re-merged onto main (the I-143 and
I-147 fields and inputs kept beside the bet's); Spanish for the bet screen, "called it" and the award.

Session B's note — **late bets must not pay**: the branch took a bet any time in the judge phase,
including the beat after the judge had already taken a card (the recording's Sam). Now
`judgePicked()` closes the bets on both sides: the server refuses one and the phone's `canGuess` goes
false. New test: a bet after the judge's pick is not recorded and pays nothing.

## Proof (probe on this worktree: TV + Sam + Priya + 2 bots, judge mode, Sam's bet placed and the

## judge made to take that card; stills in `captures/blanks/I-149-shipped/`)

- Round 1 result on the TV: "Bot 1 wins the round! … Bot 2's pick" and, inside the winning card's
  gold border under the author line, "called it: [face] Sam" (`result-tv-r1.png`).
- Round 2: Sam's roster chip reads 0.5 — the half point sits on the board without breaking it.
- Final screen, TV and Sam's phone: "Read the room · Sam — 2 calls called right".
- Blanks 154 tests (side bet + late bet); `pnpm verify` green.
