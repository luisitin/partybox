# I-030 · Pausing freezes the room — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-030.md` (the owner's pick, branch `b/I-030-C`, with the note
"when resumed, it should repeat the call it left on"). The shell part is reapplied on `session-c`
exactly as the branch has it (`TvPlaying.module.css` + the two container classes); noted in
docs/DESIGN_SYSTEM.md (the motion section — the curtain no longer fades).

The note is Bingo's: `shiftResume` in `games/bingo/server/index.ts` now hands a VIP resume that
lands in a running call (`play` with a deadline) to `resumeAfterHold` — the 3 · 2 · 1 with
`resumeAgain`, so the TV calls the number that was up again (a new stamp: drop, boing, voice)
before a full interval. The dibs window still shifts whole (loop 295), a held caller (menu open,
no deadline) stays held, and check / bingo / scoreboard keep the plain shift. The pause unit test
now proves the re-call.

## Proof (2026-09-20, the ideas lobby capture against this worktree, the pause mark stretched to 20 s)

- `curtain-drop.png` — f40 … f46 of the `pause` clip, the owner's `I-030-C` clip (top) and mine
  (bottom): the curtain slides down from the top edge over 600 ms while the stage scales back and
  the strip dims; the Paused card lands once it is down. Frame for frame the same.
- `curtain-lift.png` — the resume: the curtain lifts (f97 → f98) and the stage lands back to full
  with the 3 · 2 · 1 already showing "3" under it.
- `recall.png` — the countdown 2 · 1, then **B 8 again** ("call 2 of 75", "before that 9" — the
  number the room was on when Sam paused), then N 34 (call 3) a full interval later.
- Cue log (TV, from the mark): `pause` +2.5 s; resume ≈ +8.0 s → `tick` +8.05 / +9.06 / +10.05,
  `call` +11.25 (B 8 again), `call` +17.25 (N 34, 6 s later — the room's call interval).
- `pnpm verify` GREEN.
