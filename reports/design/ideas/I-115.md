# I-115 · A lapsed BINGO? resolves — option C (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-115.md` (the owner's pick, branch `b/I-115-C`, no note).
Cherry-picked onto `session-c` as the branch has it (conflicts with I-097/I-099 in the controller
files, both kept): the line holds a beat as "— never mind", muted, and fades (A), a quiet `bust`
with it (B), the phone's "Dibs lapsed — tap twice within 3 s to claim" for two seconds (C). Both
"the arm just went" detections are state adjusted during render, with the clock check in the
effect (the react-hooks rules: no ref read or impure call in render, no setState in an effect).

## Proof (2026-09-21, the ideas Bingo capture's lapse marks against this worktree)

- `I-115/lapse.png` — `lapse-tv` (rows 1–2: the owner's `I-115-C`, mine) and `lapse-phone`
  (rows 3–4): "Priya says BINGO?…" with the dibs bar, then "— never mind"; the phone's 3 · 2 · 1
  label, then BINGO! back with the hint under it. The same beats.
- Cue log at the mark: mine `call −0.36 · dibs 0.07 · bust 3.21`; the owner's `call −0.48 · dibs
0.07 · bust 3.34`.
- `pnpm verify` GREEN.
