# I-108 · A valid bingo with stray daubs gets a kicker — option B (shipped)

Spec: `C:\dev\partybox-ideas\specs\I-108.md` (the owner's pick, branch `b/I-108-B`, no note).
Cherry-picked onto `session-c` (a CSS tail conflict, both kept): the kicker in the caller's tone
under the winner line — the TV view gains `spicy` (A); the never-called daubs keep their red ✕ on
the winner's card through the win stage (B).

## Proof (2026-09-22, the ideas Bingo capture with STRAY=1 against this worktree)

- `I-108/check-bingo.png` — the `check-bingo` clip, the owner's `I-108-B` (top) and mine
  (bottom): the check with one red cell, then "BINGO! Sam wins round 1 · …and 1 daub that was
  never called — lucky the line was real." with the stray still red. The same beats.
- Cue log: `dibs · reveal · win`, as before.
- `pnpm verify` GREEN.
