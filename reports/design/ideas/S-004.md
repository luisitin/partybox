# S-004 · Music on the phones — option C + the owner's note (shipped)

Spec: `C:\dev\partybox-ideas\specs\S-004.md` (the owner's pick, branch `b/S-004-C`, with the note
"Make sure that the music actually plays on the phone when enabled. If VIP enables it, then it is
auto for everyone"). Reapplied on `session-c` (the shell files had moved under S-003) plus the note:

- **It plays**: the phone's engine is created with the app, unlocked by its next tap, and fed the
  room's plan from the same `/music/…` files the TV uses (this checkout has the twelve MP3s).
- **VIP → everyone**: `musicOnPhones` on the room — a VIP action `setMusicOnPhones` beside
  "Save a recap" on the phone picker and the TV's game select, allowed any time, in the
  snapshot; every phone plays when it is on, the phone's own switch stays for rooms where the VIP
  left it off. ADR-040.
- The "♪ Lobby set" line and soft / normal / loud follow the music, not only the phone's own
  switch; the engine re-levels a playing track on a level change (the branch's `play` ignored a
  same-plan call, so the level never applied).
- `musicWhat` is derived, not state set in an effect (the react-hooks rule).

## Proof (2026-09-21, `music-probe.ts` — a play-state assertion, as the note asks)

- Priya's phone with the room switch off: no `Audio` element at all. Sam (VIP) flips "Music on
  every phone"; Priya taps once → her engine's element: `/music/airport-lounge.mp3`, `paused:
false`, `currentTime 3.30 → 4.82` over 1.5 s (advancing), volume 0.24 (the lobby level × normal).
- The 🎨 sheet shows "♪ Lobby set"; tapping **loud** re-levels the same playing track to 0.35.
- `S-004/sheet.png` — the sheet with the switch, the line and the level.
- Not done here: listening on a real handset — the harness asserts play-state, not sound; the
  owner's ear is the last check.
- `pnpm verify` GREEN.
