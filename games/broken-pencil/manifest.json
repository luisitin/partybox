{
  "id": "broken-pencil",
  "name": "Broken Pencil",
  "icon": "✏️",
  "tagline": "Draw it. Guess it. Watch it fall apart.",
  "description": "Telephone with a pencil. Pick a secret word, draw it on your phone, pass the book on: the next player guesses, the one after draws that guess, and so on around the circle. Then the TV turns the pages of every book one at a time while the VIP holds the Next button. No points — the show is the game.",
  "howToPlay": [
    "Pick a secret word and draw it on your phone.",
    "Pass it on: the next player guesses it, the one after draws that guess.",
    "The TV turns every book's pages and shows how far each one drifted."
  ],
  "version": "1.0.0",
  "minPlayers": 3,
  "maxPlayers": 8,
  "estimatedMinutes": 20,
  "estimate": {
    "fixedSeconds": 30,
    "perRoundSeconds": 55,
    "perPlayerPerRoundSeconds": 4,
    "roundsSetting": "players"
  },
  "tags": ["drawing", "comedy"],
  "presence": { "needs": "anywhere" },
  "addedOn": "2026-01-01",
  "maxInputBytes": 6144,
  "settings": [
    {
      "key": "passes",
      "label": "Players per book",
      "description": "How many other players touch each book (everyone in the room by default).",
      "type": "number",
      "default": 15,
      "min": 1,
      "max": 15,
      "maxFromPlayers": -1,
      "step": 1
    },
    {
      "key": "drawSeconds",
      "label": "Drawing time",
      "description": "Seconds per drawing",
      "type": "number",
      "default": 60,
      "min": 30,
      "max": 120,
      "step": 10
    },
    {
      "key": "guessSeconds",
      "label": "Guessing time",
      "description": "Seconds per guess",
      "type": "number",
      "default": 30,
      "min": 15,
      "max": 60,
      "step": 5
    },
    {
      "key": "customWords",
      "label": "Custom words",
      "description": "Let players type their own secret word",
      "type": "boolean",
      "default": true
    },
    {
      "key": "spicy",
      "label": "Spicy words",
      "description": "Adds explicit adult words to the offers (18+, as wild as the Blanks WILD deck)",
      "type": "boolean",
      "default": false
    }
  ],
  "supportsBots": true
}
