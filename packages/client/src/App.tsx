import { PARTYBOX_VERSION } from '@partybox/shared';

// Placeholder until Phase 2 adds the route switch (/tv, /, /preview).
export function App(): React.JSX.Element {
  return <h1>PartyBox {PARTYBOX_VERSION}</h1>;
}
