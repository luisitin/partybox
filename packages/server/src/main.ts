// Entry point for `pnpm dev` / `pnpm start`. The real server lands in Phase 2.
import { PARTYBOX_VERSION } from '@partybox/shared';

console.log(`PartyBox ${PARTYBOX_VERSION}: server not implemented yet (Phase 2).`);
process.exitCode = 1;
